# 数据同步插件接口选择到同步成功流程

本文说明用户在配置页选择一个抖店接口后，到飞书多维表格完成数据同步之间发生了什么。目标读者包括产品、测试、后端和前端开发。

## 一、产品视角

用户在配置页做的事情是“保存同步配置”，不是立即把数据写入表格。

完整流程如下：

1. 用户打开飞书多维表格的数据同步插件配置页。
2. 用户选择“目标同步动作 / 模块”，例如“商品资质列表”。
3. 用户完成账号 Cookie、店铺 ID、字段映射、刷新频率等配置。
4. 用户点击“保存同步配置”。
5. 前端通过飞书 SDK `bitable.saveConfigAndGoNext` 把配置交给飞书。
6. 飞书同步引擎根据插件 `meta.json` 里的接口声明，调用后端表结构接口 `/api/table_meta`。
7. 后端按用户选择的接口返回字段结构，飞书据此创建或更新同步表字段。
8. 飞书同步引擎调用后端表记录接口 `/api/records`。
9. 后端携带用户绑定的 Cookie 请求抖店真实接口，分页拉取数据。
10. 飞书把 `/api/records` 返回的记录写入数据同步表。

因此，用户点击保存后的正确提示应该是：

```text
同步配置已保存。飞书将在创建数据源或触发刷新时调用表记录接口拉取数据。
```

如果产品希望用户在配置页中确认 Cookie 或接口是否可用，需要单独设计“测试当前接口”按钮。这个按钮属于连接器自己的调试动作，不等同于飞书正式同步。

当前“测试连接”会使用当前活跃账号，并携带页面选择的接口、纯数字 Shop ID、同步时间范围和动态 Query 参数。接口声明为必填的 `customQueryFields` 未填写时，测试和保存都会在前端阻止；后端还会再次校验 Shop ID，避免绕过页面直接提交非法参数。

## 二、飞书数据同步插件三类接口

### 1. 插件配置接口 `/meta.json`

文件位置：

```text
data-sync-be-demo/public/meta.json
data-sync-be-demo/index.js
```

作用：

- 告诉飞书这是一个 `data_connector`。
- 告诉飞书前端配置页地址 `dataSourceConfigUiUri`。
- 告诉飞书表结构接口地址 `/api/table_meta`。
- 告诉飞书表记录接口地址 `/api/records`。

当前 `meta.json` 核心结构：

```json
{
  "type": "data_connector",
  "extraData": {
    "dataSourceConfigUiUri": "https://当前域名/index.html"
  },
  "protocol": {
    "type": "http",
    "httpProtocol": {
      "uris": [
        { "type": "tableMeta", "uri": "/api/table_meta" },
        { "type": "records", "uri": "/api/records" }
      ]
    }
  }
}
```

### 2. 表结构接口 `/api/table_meta`

文件位置：

```text
data-sync-be-demo/index.js
data-sync-be-demo/table_meta.js
data-sync-be-demo/doudian_interfaces.js
data-sync-be-demo/database.js
```

调用时机：

- 用户保存配置后，飞书需要创建同步表或确认字段结构时调用。
- 该接口不拉取业务数据，只返回字段定义。

请求中的关键数据：

```json
{
  "params": "{\"datasourceConfig\":\"{...}\"}"
}
```

其中 `datasourceConfig` 就是前端 `bitable.saveConfigAndGoNext` 保存的配置。后端会从里面读取：

```json
{
  "syncModule": "doudian_shop__product_qual_list"
}
```

后端处理逻辑：

1. 从 `syncModule` 解析接口 key，例如 `product_qual_list`。
2. 查询 MySQL 表 `doudian_interfaces`。
3. 读取接口的 `fields_schema`。
4. 返回飞书要求的 `tableName` 和 `fields`。

重要字段：

```json
{
  "fieldID": "col_qual_id",
  "fieldName": "资质 ID",
  "fieldType": 1,
  "isPrimary": true
}
```

注意：

- 至少且只能有一个主键字段 `isPrimary=true`。
- 配置页不允许取消或清空主键映射；即使恢复的是旧配置，后端也会强制保留接口 Schema 声明的主键。
- 每个 `fieldID` 和 `fieldName` 都必须非空且唯一，多个源字段不能映射到同一目标列。
- `account_name` 是连接器保留字段，用于写入本次同步账号名称，业务字段不能声明或映射到该字段 ID。
- 为兼容当前飞书协议和旧 demo，后端同时返回 `fieldID` 与 `fieldId`，并同时返回 `isPrimary` 与 `is_primary`。
- 如果没有主键字段，飞书创建同步表时会报错：`don't has primary field`。

### 3. 表记录接口 `/api/records`

文件位置：

```text
data-sync-be-demo/index.js
data-sync-be-demo/table_records.js
data-sync-be-demo/dy_helper.js
data-sync-be-demo/doudian_interfaces.js
data-sync-be-demo/database.js
```

调用时机：

- 飞书创建完同步表后，开始拉取数据时调用。
- 用户手动刷新或定时刷新时也会调用。

请求中的关键数据：

```json
{
  "params": "{
    \"datasourceConfig\":\"{...}\",
    \"transactionID\":\"同步事务 ID\",
    \"pageToken\":\"上一页返回的 nextPageToken\",
    \"maxPageSize\":1000
  }"
}
```

后端处理逻辑：

1. 解析 `datasourceConfig`，拿到 `syncModule`、账号 key、店铺 ID 和字段映射；保存到飞书的配置不包含明文 Cookie。
2. 先按 `companyId + userId` 从数据库刷新当前账号，优先使用数据库里的最新 Cookie。
3. 严格解析 `pageToken`；普通分页 token 非法时直接失败，聚合接口由对应聚合处理器校验自己的游标。
4. 根据数据库接口配置调用真实抖店接口。
5. 后端会先按接口成功码规则判断当前响应是否成功；默认兼容 `0`、`200`、`100000`，也支持数据库 `request_config.successCodes` 自定义。
6. 先按 `request_config.listPaths` 提取列表；如果接口声明“列表项是 JSON 字符串”，会先把每一项 `JSON.parse` 成对象。
7. 后端再按 `fields_schema.sourcePath` 读取字段；如果字段配置里存在 `valueMap`、`enumMap` 或 `dict`，会继续把原始码值映射成可读文本；如果字段 `type=percentage`，则会把 `0.03958` 这类小数比例格式化成 `3.96%` 文本后再写入飞书。
8. 检查主键值存在且本页唯一，并阻止重复目标字段映射覆盖数据。
9. 根据抖店总数和本次拉取数量返回 `hasMore` 与 `nextPageToken`。

飞书要求的响应结构：

```json
{
  "code": 0,
  "msg": "",
  "data": {
    "nextPageToken": "page_2_100_1784000000000",
    "hasMore": true,
    "records": [
      {
        "primaryID": "7651898863322136858",
        "data": {
          "col_qual_id": "7651898863322136858",
          "col_qual_name": "洁质安发酵液",
          "col_qual_type_name": "品牌标签图"
        }
      }
    ]
  }
}
```

注意：

- `primaryID` 必须唯一，且长度不能超过 100。
- `data` 的 key 必须是 `/api/table_meta` 返回过的 `fieldID`。
- 为兼容旧 demo，后端同时补齐 `primaryID` 与 `primaryId`。

## 三、接口目录如何驱动真实同步

接口目录表：

```text
doudian_interfaces
```

核心字段：

```text
interface_key    接口唯一标识
module_group     前端分组
interface_name   前端展示名称
api_host         例如 https://fxg.jinritemai.com
api_path         例如 /product_qual/list
is_enabled       是否允许用户选择
request_config   请求方法、分页参数、响应列表路径
fields_schema    字段结构和响应取值路径
```

前端只展示 `is_enabled=1` 的接口。

## 四、商品资质列表示例

用户选择：

```text
店铺接口 -> 店铺管理 -> 商品资质列表
```

前端保存配置：

```json
{
  "syncModule": "doudian_shop__product_qual_list"
}
```

数据库接口配置：

```json
{
  "apiHost": "https://fxg.jinritemai.com",
  "apiPath": "/product_qual/list",
  "requestConfig": {
    "method": "GET",
    "pageParam": "page",
    "pageSizeParam": "size",
    "pageStart": 0,
    "pageSize": 100,
    "listPaths": ["data.brand_qual_list"],
    "totalPaths": ["data.total"]
  }
}
```

真实请求第一页：

```text
GET https://fxg.jinritemai.com/product_qual/list?page=0&size=100
Cookie: 用户绑定的抖店 Cookie
```

抖店响应：

```json
{
  "data": {
    "total": 14,
    "brand_qual_list": [
      {
        "qual_type": "6966228537426575660",
        "qual_type_name": "品牌标签图",
        "qual_id": "7651898863322136858",
        "qual_first_img": "https://...",
        "update_time": 1781596544,
        "qual_name": "洁质安发酵液",
        "qual_source": "商家自主上传"
      }
    ]
  },
  "code": 0,
  "msg": ""
}
```

字段配置：

```json
[
  { "key": "qual_id", "defaultField": "col_qual_id", "sourcePath": "qual_id", "isPrimary": true },
  { "key": "qual_name", "defaultField": "col_qual_name", "sourcePath": "qual_name" },
  { "key": "qual_type", "defaultField": "col_qual_type", "sourcePath": "qual_type" },
  { "key": "qual_type_name", "defaultField": "col_qual_type_name", "sourcePath": "qual_type_name" },
  { "key": "qual_first_img", "defaultField": "col_qual_first_img", "sourcePath": "qual_first_img" },
  { "key": "update_time", "defaultField": "col_update_time", "sourcePath": "update_time" },
  { "key": "qual_source", "defaultField": "col_qual_source", "sourcePath": "qual_source" }
]
```

返回给飞书的记录：

```json
{
  "primaryID": "7651898863322136858",
  "data": {
    "col_qual_id": "7651898863322136858",
    "col_qual_name": "洁质安发酵液",
    "col_qual_type": "6966228537426575660",
    "col_qual_type_name": "品牌标签图",
    "col_qual_first_img": "https://...",
    "col_update_time": 1781596544000,
    "col_qual_source": "商家自主上传"
  }
}
```

## 五、分页规则

这里有两层分页。

飞书分页：

```text
pageToken / nextPageToken / hasMore / maxPageSize
```

抖店分页：

```text
page / size
```

后端负责把飞书分页翻译成抖店分页。

以 `/product_qual/list` 为例：

| 飞书请求 pageToken | 后端内部页码 | 抖店请求 page | 抖店请求 size |
| ------------------ | ------------ | ------------- | ------------- |
| 空                 | 1            | 0             | 100           |
| page_2_100_1784000000000 | 2      | 1             | 100           |
| page_3_200_1784000000000 | 3      | 2             | 100           |

后端根据接口响应中的 `data.total` 判断是否还有下一页：

```text
已加载数量 = (内部页码 - 1) * size + 本次返回条数
hasMore = 已加载数量 < total
nextPageToken = hasMore ? page_(内部页码 + 1)_(累计条数)_(时间锚点) : ""
```

补充规则：

- 旧版 `page_2` 和纯数字页码仍可兼容读取，但新 token 会同时携带累计条数和 13 位毫秒时间锚点。
- 非法普通分页 token 不会回退第一页；聚合 token 还会校验来源下标、页码和当前来源偏移。
- 用户选择 `rolling` 时间范围时，第一页确定的开始/结束边界会通过 token 固定下来，后续页不会按新的当前时间重新计算。
- 聚合接口返回 `hasMore=true` 时必须同时返回有效 `nextPageToken`，否则本页按错误处理，避免下一页重复第一页。

## 六、列表项是 JSON 字符串时怎么处理

有些接口虽然 `listPaths` 可以正确命中列表，但列表中的每一项不是对象，而是 JSON 字符串，例如：

```json
[
  "{\"order_id\":\"6926380170248355402\",\"pay_type\":\"PA\"}"
]
```

默认情况下，后端不会自动解析这些字符串，而是把它们当普通字符串处理。

如果要让后端先把每一项解析成对象，再按 `fields_schema.sourcePath` 取值，需要在数据库 `doudian_interfaces.request_config` 中显式开启：

```json
{
  "listPaths": ["data"],
  "parseListItemJson": true
}
```

或者：

```json
{
  "listPaths": ["data"],
  "listItemFormat": "json_string"
}
```

开启后的处理顺序是：

1. 先按 `listPaths` 提取数组
2. 判断当前接口是否配置了 `parseListItemJson=true` 或 `listItemFormat=json_string`
3. 如果配置了，就对列表每一项执行 `JSON.parse`
4. 再按 `fields_schema.sourcePath` 读取字段，例如 `order_id`、`pay_type`

如果没有配置这个开关，就仍然按普通对象数组处理。

## 七、开发新增一个接口时怎么做

1. 在 `data-sync-be-demo/doudian_interfaces.js` 中新增接口 seed。
2. 设置 `interfaceKey`、`moduleGroup`、`interfaceName`、`apiPath`、`isEnabled`。
3. 配置 `requestConfig`：

```json
{
  "method": "GET",
  "pageParam": "page",
  "pageSizeParam": "size",
  "pageStart": 0,
  "pageSize": 100,
  "listPaths": ["data.xxx_list"],
  "totalPaths": ["data.total"]
}
```

### `requestConfig` 配置规范

这一段非常重要。后端是否能把接口请求出去、能不能正确解析响应，主要都看这里。

可以把 `requestConfig` 理解成两部分：

1. 入参配置：请求怎么发
2. 出参配置：响应怎么读

#### 1. 入参配置

常用字段说明如下：

| 字段 | 含义 | 是否必填 | 说明 |
| ---- | ---- | ---- | ---- |
| `method` | 请求方法 | 是 | `GET` 或 `POST` |
| `contentType` | 请求体类型 | 否 | 默认 `application/json;charset=UTF-8` |
| `extraQuery` | 固定 Query 参数 | 否 | 会并到请求参数中 |
| `extraBody` | 固定 Body 参数 | 否 | 仅对 POST / 非 GET 请求有意义 |
| `pageParam` | 页码参数名 | 分页接口必填 | 如 `page`、`page_no` |
| `pageSizeParam` | 每页大小参数名 | 分页接口必填 | 如 `pageSize`、`size`、`page_size` |
| `pageStart` | 分页起始值 | 分页接口必填 | 常见是 `0` 或 `1` |
| `pageSize` | 默认分页大小 | 分页接口必填 | 会受飞书 `maxPageSize` 限制 |
| `pagination` | 是否分页 | 否 | 不写默认 `true`；写 `false` 表示不分页 |
| `includeShopId` | 是否自动注入店铺 ID | 否 | 打开后会自动传 `shop_id` 和 `shopId` |
| `requiredParams` | 参数说明清单 | 强烈建议填写 | 给人看、给页面看，不直接决定真实拼参逻辑 |
| `dateRangeMapping` | 同步时间范围映射 | 否 | 把页面里的时间范围自动翻译成真实请求参数 |
| `customQueryFields` | 自定义查询字段定义 | 否 | 用于页面动态渲染输入框 |
| `referer` | 指定 Referer | 否 | 某些接口需要特定来源页时使用 |
| `extraHeaders` | 额外请求头 | 否 | 某些接口需要特定 Header 时使用 |
| `includeOriginHeader` | 是否携带 Origin | 否 | 默认不带，只有特殊接口才打开 |

`includeOriginHeader=true` 对 GET 和 POST 都生效；未开启时后端会主动移除 `Origin`，避免无意携带错误来源。

#### 2. `method` 和参数位置的关系

`method` 不只是顶部换一下，它决定参数放哪：

- `GET`：参数会拼到 URL Query 上
- `POST`：参数会放进请求体里

`page.current`、`page.size` 这类点分分页路径会先构造成嵌套对象。发送 GET Query 或
`application/x-www-form-urlencoded` 时，后端再稳定展开为：

```text
page.current=1&page.size=100
```

不会再把对象编码成 `[object Object]`。数组基础值使用同名参数重复传递，数组中的对象值使用 JSON 字符串传递。

点分路径不能包含 `__proto__`、`prototype` 或 `constructor`，普通分页页码和累计条数也有上限；非法配置会明确失败，不会继续构造请求。

但是参数名不是看 `requiredParams`，而是看：

```json
{
  "pageParam": "page",
  "pageSizeParam": "pageSize"
}
```

例如：

```json
{
  "method": "POST",
  "pageParam": "page_no",
  "pageSizeParam": "page_size"
}
```

后端真正发出去的就是：

```json
{
  "page_no": 1,
  "page_size": 100
}
```

#### 3. 分页接口怎么配

分页接口示例：

```json
{
  "method": "GET",
  "pageParam": "page",
  "pageSizeParam": "pageSize",
  "pageStart": 0,
  "pageSize": 100,
  "extraQuery": {},
  "listPaths": ["data.list"],
  "totalPaths": ["data.total"]
}
```

说明：

- 第一页会发 `page=0&pageSize=100`
- 第二页会发 `page=1&pageSize=100`
- 后端根据 `totalPaths` 解析总数，再决定 `hasMore`

#### 4. 不分页接口怎么配

如果接口本身一次查全量，没有 `page / size` 这种参数，就明确写：

```json
{
  "method": "GET",
  "pagination": false,
  "extraQuery": {},
  "listPaths": ["data"],
  "totalPaths": []
}
```

规则：

- `pagination` 不写时，默认按 `true` 处理
- `pagination: false` 时，后端不会自动传分页参数
- 飞书侧也只会拉一次，`hasMore=false`

#### 5. 同步时间范围怎么配

页面上的“同步时间范围”是一个公共能力。它不是每个接口都自己造轮子，而是通过 `dateRangeMapping` 映射到真实参数名。

例如：

```json
{
  "dateRangeMapping": {
    "startTime": "audit_start_time",
    "endTime": "audit_end_time",
    "format": "date"
  }
}
```

如果用户选择“近 7 天”，后端会自动算出：

```json
{
  "audit_start_time": "2026-06-26",
  "audit_end_time": "2026-07-02"
}
```

支持的 `format`：

- `date`：`2026-07-02`
- `datetime`：`2026-07-02 23:59:59`
- `timestamp_ms`：毫秒时间戳
- `timestamp_s`：秒时间戳

如果用户选择“全量数据”，则不会传这两个时间参数。

#### 6. 自定义查询字段怎么配

不同接口如果除了分页、时间范围，还需要用户自己补查询条件，可以配：

```json
{
  "customQueryFields": [
    {
      "name": "created_at_range",
      "label": "创建时间范围",
      "type": "dateRange",
      "startTime": "start_time",
      "endTime": "end_time",
      "format": "datetime_minute",
      "maxPastMonths": 6,
      "endPastMonths": 6,
      "required": true
    },
    {
      "name": "status",
      "label": "状态",
      "type": "string",
      "options": [
        { "label": "全部", "value": "all" },
        { "label": "成功", "value": "success" }
      ]
    }
  ]
}
```

它的本质是“页面表单定义”，不是直接发请求的数据本身。

最终用户填完后，会汇总进运行时参数里。

支持的 `type`：

- `string`：普通输入框
- `integer` / `number`：数字输入框
- `boolean`：是/否下拉框
- `dateRange`：日期范围选择器，页面选一个范围，最终拆成 `startTime` 和 `endTime` 两个请求参数

`dateRange` 额外配置：

- `startTime` / `endTime`：真实请求里的开始/结束参数名
- `format`：输出格式，支持 `date`、`datetime`、`datetime_minute`、`timestamp_ms`、`timestamp_s`
- `maxPastMonths` / `maxPastDays`：可选窗口长度
- `endPastMonths` / `endPastDays`：可选窗口的截止日期距离今天多远，不填时截止到今天

最小 demo：

```json
{
  "name": "order_time_range",
  "label": "订单时间",
  "type": "dateRange",
  "startTime": "start_time",
  "endTime": "end_time",
  "format": "datetime",
  "maxPastMonths": 6,
  "endPastMonths": 6,
  "required": true
}
```

如果今天是 `2026-07-17`，这个配置只能选择 `2025-07-17` 到 `2026-01-17`，`2026-01-18` 之后会禁用。

用户选择 `2025-12-01` 到 `2025-12-02` 后，请求里会追加：

```json
{
  "start_time": "2025-12-01 00:00:00",
  "end_time": "2025-12-02 23:59:59"
}
```

字段说明：

| 字段 | 必填 | 说明 |
|---|---|---|
| `name` | 是 | 前端保存这个日期范围控件状态用的内部 key，不能和其他自定义字段重复。 |
| `label` | 否 | 页面表单展示名，不填时展示 `name`。 |
| `type` | 是 | 固定写 `dateRange`，表示渲染日期范围选择器。 |
| `startTime` | 是 | 用户选择的开始日期最终写入请求的参数名。 |
| `endTime` | 是 | 用户选择的结束日期最终写入请求的参数名。 |
| `format` | 否 | 输出格式，默认 `date`。可选 `date`、`datetime`、`datetime_minute`、`timestamp_ms`、`timestamp_s`。不是 `date` 时会显示时间选择。 |
| `maxPastDays` | 否 | 可选窗口长度，单位天。 |
| `maxPastMonths` | 否 | 可选窗口长度，单位月。和 `maxPastDays` 同时配置时，优先用 `maxPastDays`。 |
| `endPastDays` | 否 | 可选窗口截止日期距离今天的天数。 |
| `endPastMonths` | 否 | 可选窗口截止日期距离今天的月份数。和 `endPastDays` 同时配置时，优先用 `endPastDays`。 |
| `required` | 否 | 是否必填，默认 `false`。 |
| `helpText` | 否 | 表单下方的提示文案。 |

#### 7. `requiredParams` 的真实作用

这个字段很容易被误解。

它的主要作用是：

- 描述这个接口需要哪些参数
- 让配置更好读
- 让页面后续有机会根据它扩展 UI

但是当前后端真正拼分页参数时，主要还是看：

- `pageParam`
- `pageSizeParam`
- `pageStart`
- `pageSize`
- `pagination`

所以不要以为只改了：

```json
{
  "requiredParams": [
    { "name": "page_no" }
  ]
}
```

后端就会自动改发 `page_no`。不是的。要改就必须同步改：

```json
{
  "pageParam": "page_no"
}
```

#### 8. 出参配置

出参配置主要就是告诉后端：

- 列表数据在哪
- 总数在哪

常用字段：

| 字段 | 含义 | 示例 |
| ---- | ---- | ---- |
| `listPaths` | 明细列表路径数组 | `["data.list"]` |
| `totalPaths` | 总数字段路径数组 | `["data.total", "total"]` |

例如响应是：

```json
{
  "data": {
    "items": [
      { "id": "1", "name": "A" }
    ],
    "total": 100
  }
}
```

配置应写：

```json
{
  "listPaths": ["data.items"],
  "totalPaths": ["data.total"]
}
```

#### 9. `sourcePath` 怎么写

字段配置里的 `sourcePath` 支持：

- 普通对象路径：`data.name`
- 嵌套对象路径：`suggest_advice_info.title`
- 数组下标路径：`suggest_advice_info.advice_info[0].reduce_price`
- 数组通配路径：`orderList[*].skuOrderInfoList[*].productName`

例如：

```json
{
  "key": "reduce_price",
  "type": "price",
  "sourcePath": "suggest_advice_info.advice_info[0].reduce_price"
}
```

现在后端支持两类数组写法：

- `[0]`：只读取数组第一项
- `[*]` 或 `[]`：读取数组所有项

例如订单里有多个商品或多个 SKU 时，不要写死：

```json
{
  "sourcePath": "orderList[0].skuOrderInfoList[0].productName"
}
```

而应该写成：

```json
{
  "sourcePath": "orderList[*].skuOrderInfoList[*].productName"
}
```

通配数组字段写入飞书时：

- 文本字段会把多个值用 `、` 拼接
- `price` 字段会把多个金额按“分转元”后求和
- `Number` 字段会求和
- `DateTime` 和链接字段默认取第一个有效值
- 可通过 `arrayMode: "first"` 强制取第一个
- 可通过 `arrayMode: "json"` 保留原始数组 JSON
- 可通过 `arrayJoiner` 自定义文本拼接分隔符

#### 10. 字段类型怎么配

常用字段类型如下：

| 类型 | 含义 | 后端写入飞书时的行为 |
| ---- | ---- | ---- |
| `Text` | 文本 | 转字符串 |
| `Number` | 数字 | 转数字 |
| `DateTime` | 日期时间 | 转毫秒时间戳 |
| `price` | 金额 | 默认按“分转元”再写入数字列 |
| `ImageUrl` | 图片链接 | 转飞书超链接对象 |
| `VideoUrl` | 视频链接 | 转飞书超链接对象 |

注意：

- `price` 会按 `value / 100` 处理
- 所以数据库里如果本来就是元，就不要配 `price`
- `price` 在 `table_meta` 中也会按数字列返回

#### 11. 一个完整示例

```json
{
  "method": "GET",
  "pageParam": "page",
  "pageSizeParam": "pageSize",
  "pageStart": 0,
  "pageSize": 100,
  "extraQuery": {},
  "extraBody": {},
  "listPaths": ["data.list"],
  "totalPaths": ["data.total"],
  "dateRangeMapping": {
    "startTime": "audit_start_time",
    "endTime": "audit_end_time",
    "format": "date"
  },
  "customQueryFields": [
    {
      "name": "status",
      "label": "状态",
      "type": "string"
    }
  ],
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

#### 12. 排查顺序建议

如果一个接口配完后不工作，建议按下面顺序检查：

1. `method` 是否对
2. `pageParam` / `pageSizeParam` 是否和真实接口一致
3. `pageStart` 是 `0` 还是 `1`
4. `listPaths` / `totalPaths` 是否命中真实响应
5. 是否需要 `dateRangeMapping`
6. 是否需要 `customQueryFields`
7. 是否其实是不分页接口，应配 `pagination: false`
8. 是否需要 `referer` / `extraHeaders`
9. `sourcePath` 是否写错，尤其是数组路径
10. `isPrimary` 是否只保留一个主键字段

一句话总结：

- 入参怎么发，看 `method + pageParam/pageSizeParam + extraQuery/extraBody + pagination + dateRangeMapping`
- 出参怎么读，看 `listPaths + totalPaths + sourcePath`

4. 配置 `fields`，每个字段必须包含：

```json
{
  "key": "qual_id",
  "label": "资质 ID (qual_id)",
  "type": "Text",
  "defaultField": "col_qual_id",
  "fieldName": "资质 ID",
  "sourcePath": "qual_id",
  "isPrimary": true
}
```

5. 重启后端，让 `initDb()` 同步接口目录。
6. 前端刷新配置页，选择该接口。
7. 检查“字段与同步配置”是否展示真实字段。
8. 保存配置后，飞书调用 `/api/table_meta` 与 `/api/records` 完成同步。

## 八、常见问题

### 1. 保存后为什么没有马上看到数据？

因为保存只是调用 `bitable.saveConfigAndGoNext` 保存配置。真正同步由飞书同步引擎调用 `/api/records` 触发。

### 2. 报错 `don't has primary field` 是什么原因？

飞书没有在 `/api/table_meta` 的字段列表里识别到主键字段。需要确认：

- 必须且只能有一个字段 `isPrimary=true`。
- 字段同时返回 `fieldID`。
- 主键字段类型是文本、数字、日期、超链接、电话号码、条码或货币之一。

### 3. Cookie 在哪里使用？

Cookie 加密保存在账号表中，不写入飞书保存的任务配置。飞书调用 `/api/records` 时，后端根据配置中的账号 key 和当前用户权限从数据库刷新最新凭证，再请求抖店真实接口。

### 4. 调试工具成功，但飞书同步提示 Cookie 过期怎么办？

先不要直接判断 Cookie 真的过期。抖店后台接口可能同时依赖 Cookie 和 Query 风控参数。

以 `/product_qual/list` 为例，调试工具中除了：

```text
page=0
size=200
```

还可能带有：

```text
_bid=...
verifyFp=...
fp=...
msToken=...
a_bogus=...
```

如果后端同步时只带 Cookie 和 `page/size`，没有带这些 Query 参数，抖店可能返回 403、HTML 登录/风控页或非 JSON 内容。旧逻辑会把这类情况误报为 Cookie 过期；现在后端会区分：

- 401 或明确登录失效：`凭证失效(Cookie过期)`
- 403、HTML、非 JSON：`抖店接口请求参数不完整`

配置页中“接口附加 Query 参数”用于复制调试工具里成功请求的额外 Query 参数，例如：

```text
_bid=ffa_governance_new&verifyFp=xxx&fp=xxx&msToken=xxx&a_bogus=xxx
```

分页参数 `page` 和 `size` 不需要填写在这里，后端会按飞书分页自动生成。

### 5. 不同接口调用方式不同怎么办？

简单差异放在数据库配置里，例如 GET/POST、page/size、listPaths、totalPaths。

复杂差异放代码 handler 里，例如枚举循环、特殊签名、特殊 Header、特殊字段清洗。
