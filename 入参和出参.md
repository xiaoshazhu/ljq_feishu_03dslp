对，这个思路是对的，我已经按这个方向接进去了，逻辑在 [dy_helper.js](/Users/ly/connector/feishu-doudian-connector/data-sync-be-demo/dy_helper.js)。

现在你可以在 `request_config` 里这样配一个公共时间范围映射：

```json
{
  "dateRangeMapping": {
    "startTime": "start_time",
    "endTime": "end_time",
    "format": "datetime"
  }
}
```

支持的 `format` 现在有：

- `datetime` -> `2026-07-02 23:59:59`
- `date` -> `2026-07-02`
- `timestamp_ms` -> `1751471999000`
- `timestamp_s` -> `1751471999`

当前默认规则是 `natural_day`。  
也就是如果用户在 **2026-07-02** 选择“7天”：

- `start_time = 2026-06-26 00:00:00`
- `end_time = 2026-07-02 23:59:59`

如果你配的是：

```json
"dateRangeMapping": {
  "startTime": "start_date",
  "endTime": "end_date",
  "format": "date"
}
```

那就会变成：

```json
{
  "start_date": "2026-06-26",
  "end_date": "2026-07-02"
}
```

而且这个公共时间范围会先算出来，再并进真实请求参数里；你手动填的自定义参数如果同名，仍然会覆盖它。

"valueMap": {
"2": "已启用",
"4": "已停用",
"5": "已冻结"
},

{
"key": "img",
"type": "ImageUrl",
"label": "产品图片(img)",
"fieldName": "产品图片",
"sourcePath": "product.img",
"defaultField": "col_img"
},
{
"key": "playUrl",
"type": "VideoUrl",
"label": "视频地址(playUrl)",
"fieldName": "视频地址",
"sourcePath": "playInfo.playUrl",
"defaultField": "col_playUrl"
}


//自定义查询
入参
"customQueryFields": [
{
"name": "start_date",
"label": "开始时间",
"type": "string",
"required": false,
"placeholder": "例如 2026-07-01 00:00:00"
},
{
"name": "end_date",
"label": "结束时间",
"type": "string",
"required": false
},
{
"name": "status",
"label": "状态",
"type": "string",
"options": [
{ "label": "全部", "value": "all" },
{ "label": "成功", "value": "success" }
]
}


有些接口有校验

"referer": "https://fxg.jinritemai.com/ffa/g/list",
"extraHeaders": {
"X-Requested-With": "XMLHttpRequest"
}