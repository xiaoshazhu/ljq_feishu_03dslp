
 `request_config` 里这样配一个公共时间范围映射：

```json
{
  "dateRangeMapping": {
    "startTime": "start_time",
    "endTime": "end_time",
    "format": "datetime"
  }
}
```

支持的 `format` 现在有：

**- `datetime` -> `2026-07-02 23:59:59`**
- `date` -> `2026-07-02`
- `timestamp_ms` -> `1751471999000`
- `timestamp_s` -> `1751471999`

当前默认规则是 `natural_day`。  
也就是如果用户在 **2026-07-02** 选择“7天”：

- `start_time = 2026-06-26 00:00:00`
- `end_time = 2026-07-02 23:59:59`

如果你配的是：

```json
"dateRangeMapping": {
  "startTime": "start_date",
  "endTime": "end_date",
  "format": "date"
}
```

那就会变成：

```json
{
  "start_date": "2026-06-26",
  "end_date": "2026-07-02"
}
```

而且这个公共时间范围会先算出来，再并进真实请求参数里；你手动填的自定义参数如果同名，仍然会覆盖它。

"valueMap": {
"2": "已启用",
"4": "已停用",
"5": "已冻结"
},

{
"key": "img",
"type": "ImageUrl",
"label": "产品图片(img)",
"fieldName": "产品图片",
"sourcePath": "product.img",
"defaultField": "col_img"
},
{
"key": "playUrl",
"type": "VideoUrl",
"label": "视频地址(playUrl)",
"fieldName": "视频地址",
"sourcePath": "playInfo.playUrl",
"defaultField": "col_playUrl"
}


//自定义查询
入参
"customQueryFields": [
{
"name": "created_at_range",
"label": "创建时间范围",
"type": "dateRange",
"startTime": "start_time",
"endTime": "end_time",
"format": "datetime_minute",
"maxPastMonths": 6,
"endPastMonths": 6,
"required": false
},
{
"name": "status",
"label": "状态",
"type": "string",
"options": [
{ "label": "全部", "value": "all" },
{ "label": "成功", "value": "success" }
]
}

`dateRange` 会拆成 `startTime` / `endTime` 两个请求参数；`format` 支持 `date`、`datetime`、`datetime_minute`、`timestamp_ms`、`timestamp_s`，不是 `date` 时会显示时间选择；`maxPastMonths` / `maxPastDays` 表示可选窗口长度，`endPastMonths` / `endPastDays` 表示窗口截止日期距离今天多远。

最小 demo：

```json
{
  "name": "order_time_range",
  "label": "订单时间",
  "type": "dateRange",
  "startTime": "start_time",
  "endTime": "end_time",
  "format": "datetime",
  "maxPastMonths": 6,
  "endPastMonths": 6,
  "required": true
}
```

如果今天是 `2026-07-17`，只能选 `2025-07-17` 到 `2026-01-17`；选 `2025-12-01` 到 `2025-12-02` 会追加 `start_time=2025-12-01 00:00:00`、`end_time=2025-12-02 23:59:59`。


有些接口有校验

"referer": "https://fxg.jinritemai.com/ffa/g/list",
"extraHeaders": {
"X-Requested-With": "XMLHttpRequest"
}



pagination: false 表示不分页 只拉去一次

// 百分比文本字段
{
"key": "ctr",
"type": "percentage",
"fieldName": "点击率",
"sourcePath": "ctr",
"defaultField": "col_ctr",
"percentageDigits": 2
}

当接口原始值是 `0.03958` 时，后端会写入飞书文本值 `3.96%`。
