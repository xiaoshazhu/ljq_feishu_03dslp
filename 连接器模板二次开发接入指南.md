# 飞书数据连接器模板二次开发接入指南

本文给拿到本仓库、准备开发其他平台连接器的工程师使用。当前项目是“飞书多维表格 + 抖店后台 Cookie”的实现，不是已经完全抽象好的通用连接器。飞书协议、安全防护、租户隔离和分页框架可以复用；平台登录、接口目录、请求适配和大量抖店命名必须替换。

## 零、拉下代码后的第一件事：更换数据库

**不要先启动后端。必须先创建并切换到新连接器自己的数据库。** 后端启动时会执行 `database.js` 的 `initDb()`，自动检查、创建或迁移表；如果仍然连接原抖店数据库，可能把新项目的表结构和测试数据写进原库。

操作顺序：

1. 创建新数据库，不要与现有抖店连接器共库。
2. 创建该数据库的独立最小权限账号，不要复用线上管理员账号。
3. 复制 `feishu-doudian-connector-server/.env.example` 为新环境配置。
4. 首先修改 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_USER`、`MYSQL_PASSWORD`、`MYSQL_DATABASE`。
5. 人工确认 `MYSQL_DATABASE` 是新库，并确认新账号无法访问原抖店数据库。
6. 完成以上检查后，才允许第一次启动后端，让 `initDb()` 初始化新库。

禁止直接复制原项目的 `.env`、`.env.production` 或数据库密码。数据库切换没有确认前，不要执行 `npm start`、`npm run start:staging` 或生产启动命令。

## 一、先决定新平台的接入方式

开始改代码前，先确认下面四项并写进新平台的接口文档：

1. 认证方式：Cookie、OAuth、API Key，还是平台开放接口 Access Token。
2. 数据接口：官方开放 API，还是登录后的管理后台接口。
3. 账号唯一标识：店铺 ID、企业 ID、应用 ID，还是其他字段。
4. 凭证刷新方式：手动重新登录、Refresh Token 自动续期，还是长期密钥。

如果新平台不是“浏览器登录后捕获 Cookie”，不要保留书签捕获流程硬套使用。飞书协议层可以继续复用，账号表和认证适配器需要按实际认证方式调整。

## 二、拿到代码后的首轮修改清单

按下面顺序处理，不建议全仓库直接批量替换。

### 1. 新建独立项目和独立配置

- 确认已经完成第零节的数据库切换，再配置新的仓库、部署目录和域名。
- 只复制 `.env.example` 生成新环境配置，不要复制已有 `.env`、`.env.production` 或线上密钥。
- 重新生成 `SECRET_KEY`、`MANAGEMENT_IDENTITY_SECRET`、`CREDENTIAL_ENCRYPTION_KEY` 和 `LOCAL_AGGREGATE_TOKEN`，不同用途不能共用密钥。
- 不要让新连接器和抖店连接器共用 `accounts`、`tasks`、`sync_logs` 和接口注册表，除非已经明确设计了跨平台数据隔离与迁移方案。

### 2. 修改产品名称和平台标识

先运行：

```bash
rg -n "抖店|抖音|Doudian|doudian|douyin|DOUDIAN|jinritemai" . \
  -g '!node_modules' -g '!dist' -g '!*.zip'
```

逐项判断后修改，至少包括：

| 位置 | 当前含义 | 新平台要修改什么 |
| --- | --- | --- |
| `README.md` | 抖店连接器说明 | 项目名、平台名、部署说明 |
| `data-sync-fe-vue-demo/index.html` | 配置台网页标题 | 新连接器名称 |
| `data-sync-fe-vue-demo/src/App.vue` | 页面文案、平台值、登录地址 | 新平台名称、账号字段和操作提示 |
| `data-sync-fe-vue-demo/src/assets/` | 图标和空状态图片 | 新平台素材 |
| `feishu-doudian-connector-server/public/meta.json` | 飞书连接器协议元数据 | 版本、UI 地址和同步开关 |
| `feishu-doudian-connector-server/table_meta.js` | 飞书表名 `抖店-xxx` | 新平台表名前缀 |
| `feishu-doudian-connector-server/connector_fields.js` | “抖店账号名称”说明 | 新平台账号名称 |
| `feishu-doudian-connector-server/doudian_interface_utils.js` | `doudian_shop__` 模块前缀 | 新平台稳定前缀，例如 `jushuitan__` |
| 后端文件名和函数名 | `Doudian`/`doudian` | 按平台语义重命名，避免半年后混淆 |

飞书固定协议路径 `/api/table_meta`、`/api/records` 以及 `meta.json` 中的 `tableMeta`、`records` 类型不要随平台名称修改。

## 三、登录与账号状态：必须一起修改的五个环节

当前实现的“已登录”不是飞书用户登录，而是“是否取得并验证了可调用抖店接口的商家凭证”。新平台接入时必须同时检查以下五处。

### 1. 登录入口

当前登录页写死在：

```text
data-sync-fe-vue-demo/src/App.vue
handleStartSimulatedLogin()
```

目前打开 `https://fxg.jinritemai.com/...`。需要替换成新平台官方登录页；如果新平台采用 OAuth，应改成 OAuth 授权跳转和回调流程，而不是打开后台首页。

### 2. 凭证提取

当前书签脚本位于：

```text
data-sync-fe-vue-demo/src/App.vue
buildBookmarkCode()
```

当前逻辑：

- 读取 `document.cookie`。
- 从 `shop_id` 或 `shop_id_str` 中提取店铺 ID。
- 通过 `/capture-relay.html` 把 Cookie 交给后端一次性捕获会话。

新平台必须确认：

- JavaScript 是否能读取登录凭证；`HttpOnly` Cookie 无法通过 `document.cookie` 获取。
- 账号 ID 在 Cookie、URL、页面接口还是 OAuth 返回值中。
- 是否需要同时保存 User-Agent、CSRF Token 或其他 Header。
- 是否允许用户手动粘贴凭证。

仅判断 `document.cookie` 非空不能证明用户已登录，只表示页面存在可读取 Cookie。

### 3. 登录/捕获白名单

当前有三类白名单，含义不同，不能只改其中一个：

| 环境变量 | 用途 | 新平台修改方式 |
| --- | --- | --- |
| `FRONTEND_PUBLIC_URL` | 配置台管理 API 的同源/CORS 校验 | 改成新配置台的 HTTPS Origin |
| `DOUDIAN_CAPTURE_ALLOWED_ORIGINS` | 捕获中转页允许接收凭证消息的平台页面 Origin | 改名并配置新平台登录/后台 Origin |
| `DOUDIAN_ALLOWED_API_ORIGINS` | 后端主动请求第三方 API 的 SSRF 白名单 | 改名并列出所有真实 API Origin |

相关代码：

- `index.js`：`getAllowedCaptureOrigins()`、`requireSameOrigin()`、`buildCaptureRelayHtml()`。
- `runtime_config.js`：`getDoudianAllowedApiOrigins()`。
- `http_client.js`：`assertAllowedUrl()`。

注意：`/api/v1/connector/sources/login-capture` 依靠短效一次性 token 保护，当前 CORS 会响应请求 Origin；`DOUDIAN_CAPTURE_ALLOWED_ORIGINS` 主要限制中转页接受哪个页面的 `postMessage`。不要误以为它会直接拒绝所有非白名单 POST。

白名单只填写 Origin，例如：

```text
https://seller.example.com
https://api.example.com
```

不要填写接口路径、通配符，也不要为了省事允许任意 Origin。

### 4. 判断账号是否仍然登录

当前真实状态探测链路：

```text
App.vue 页面初始化/刷新
  -> POST /api/v1/connector/accounts/refresh-status
  -> index.js 中的 accountStatusProbeInterfaceKey
  -> dy_helper.js 的 probeDoudianInterfaceSuccessCode()
  -> 请求数据库注册的探测接口
  -> 响应 code === 0 时标记 active，否则 expired
```

新平台必须修改：

1. `index.js` 中硬编码的 `accountStatusProbeInterfaceKey`。
2. 数据库接口注册表中对应的轻量探测接口。
3. `probeDoudianInterfaceSuccessCode()` 的成功条件。不要默认所有平台成功码都是 `0`。
4. 探测请求需要的认证 Header、请求参数和响应解析。

探测接口应满足：请求轻、权限要求低、不会写数据、能可靠区分“登录有效”和“业务无数据”。

### 5. 同步过程中识别登录失效

当前 `dy_helper.js` 通过下面信息判断抖店 Cookie 失效：

- HTTP 或业务码：`401`、`40004`、`10008`。
- 错误文案包含“登录”“会话”“expire”“失效”“未授权”。
- Cookie 为空、`mock_` 前缀或长度小于 30。

新平台必须根据官方接口实际响应替换这些规则。推荐优先判断稳定的 HTTP 状态码和业务错误码，错误文案只能作为兼容兜底。

账号保存成功时设置 `active` 不等于凭证已经验证成功。必须保留“保存后探测”或“首次请求验证”中的至少一种真实校验。

## 四、飞书用户身份与第三方平台登录是两回事

不要把下面两种身份混在一起：

| 身份 | 当前来源 | 用途 |
| --- | --- | --- |
| 飞书企业/用户 | `bitable.getTenantKey()`、`bridge.getBaseUserId()`，生产由可信网关签名 | 多租户、个人账号和共享账号隔离 |
| 第三方商家账号 | Cookie 捕获或手动凭证 | 请求平台数据接口 |

生产环境管理 API 必须保持：

- `MANAGEMENT_IDENTITY_AUTH_MODE=gateway_hmac`。
- 网关从可信登录会话获得企业和用户身份。
- 网关删除客户端伪造的 `X-Connector-*` Header，再注入并签名真实身份。
- 后端拒绝 `default`、`unknown` 身份。

开发环境可以临时使用 `MANAGEMENT_IDENTITY_AUTH_MODE=none`，但不能照搬到生产。

## 五、第三方数据接口适配

当前核心调用链如下：

```text
飞书 /api/records
  -> table_records.js：读取任务配置、账号、分页 token
  -> dy_helper.js：构造第三方请求、发送请求、解析列表
  -> database.js：读取 doudian_interfaces 的 request_config/fields_schema
  -> table_records.js：转换成飞书 records
```

开发新平台时至少修改：

1. 接口注册表的表名、平台过滤值和查询函数。
2. 模块前缀判断函数。
3. 请求认证方式：Cookie Header、Bearer Token、签名或 API Key。
4. 分页：页码、游标、是否还有下一页、总数路径。
5. 请求时间范围：近 N 天还是固定日期；新一轮同步应重新计算相对时间。
6. 响应列表和总数字段路径。
7. 业务错误码、限流码、凭证过期码和可重试规则。
8. 主键生成规则，确保跨页和多次同步稳定且不重复。
9. 本地聚合接口；新平台不需要时不要复制抖店专用聚合逻辑。

如果新平台接口结构与当前数据库驱动模式相近，可以继续使用 `request_config` 和 `fields_schema`；否则新增最小的平台适配函数，不要在抖店函数里堆叠大量 `if (platform === ...)`。

## 六、数据库处理

当前 `database.js` 会初始化：

- `accounts`：第三方账号和加密凭证。
- `captured_buffer`、`capture_sessions`：短效凭证捕获。
- `account_selections`：用户当前选择的账号。
- `tasks`：任务配置快照，不保存 Cookie。
- `sync_logs`、`errors`：同步日志和错误。
- `doudian_interfaces`：抖店接口注册表。

新平台处理原则：

- Cookie 型平台可以复用账号表结构，但应把展示文案和字段注释改成新平台语义。
- OAuth 平台至少需要 Access Token、Refresh Token、过期时间和刷新失败状态，不能只塞进 `cookie` 字段后假装完成。
- API Key 平台要明确 Key ID、Secret 的加密范围和轮换方式。
- 接口注册表应改为新平台名称或设计为真正的通用表，并提供兼容迁移；不要直接删除生产旧表。
- 所有凭证继续使用 `credential_cipher.js` 加密，`CREDENTIAL_ENCRYPTION_KEY` 一旦用于生产数据就不能随意更换。

数据库结构变更必须先备份，采用新增字段、回填、切换读取、最后清理的兼容迁移方式。

## 七、环境变量逐项确认

后端以 `feishu-doudian-connector-server/.env.example` 为基准。新连接器上线前至少确认：

- `NODE_ENV`、`PORT`、`FRONTEND_PUBLIC_URL`、`FRONTEND_DEV_SERVER`。
- `MYSQL_*` 使用新数据库和最小权限账号。
- `SECRET_KEY` 与飞书连接器配置一致。
- `FEISHU_APP_ID`、`FEISHU_APP_SECRET` 是否需要读取企业名称。
- `MANAGEMENT_IDENTITY_*` 已由可信网关支持。
- `CREDENTIAL_ENCRYPTION_KEY` 为新生成且独立的 32 字节密钥。
- `LOCAL_AGGREGATE_*` 是否仍需要。
- 捕获 Origin 和第三方 API Origin 白名单已换成新平台。
- 并发、超时、单页上限符合新平台限流规则。
- 生产环境保持 `ALLOW_MOCK_DATA=false`。

前端以 `data-sync-fe-vue-demo/.env.example` 为基准：

- `VITE_API_BASE_URL`：后端公网地址；同源部署可留空。
- `VITE_DOUDIAN_LOCAL_AGGREGATE_HOST`：如继续使用聚合接口，应改名并改成新平台后端地址；不使用则删除相关依赖代码和变量。

## 八、哪些飞书协议能力应保留

以下能力与第三方平台无关，优先保留：

- `/api/table_meta`、`/api/records` 协议结构。
- `request_sign.js` 的飞书原始 Body 签名校验。
- `management_identity.js` 的管理身份 HMAC 校验。
- 企业、用户、私有账号和共享账号的数据隔离。
- 凭证加密、响应大小限制、超时、限流和并发背压。
- `pageToken` 分页连续性和同一轮同步的时间锚点。
- `connector_fields.js` 中唯一主键和公共账号列机制；文案可改，约束不要删。
- `/healthz`、`/readyz`、同步日志和安全错误归一化。

## 九、第三方接口文档模板

每接入一个第三方接口，都在项目文档中保存以下内容。不要只把抓包结果放在聊天或个人电脑里。

````markdown
## 接口名称

### 用途
- 业务场景：
- 是否只读：
- 账号权限要求：

### 请求
- Method：GET/POST
- Origin：https://api.example.com
- Path：/xxx/list
- 认证：Cookie/Bearer/API Key/签名
- Content-Type：
- 分页参数：
- 时间参数及格式：
- 其他必填 Header/Query/Body：

请求示例（凭证必须脱敏）：

```json
{}
```

### 返回

成功示例：

```json
{}
```

- 列表路径：
- 总数路径：
- 是否还有下一页：
- 稳定主键字段：

### 错误情况

| HTTP/业务码 | 含义 | 是否重试 | 前端提示 |
| --- | --- | --- | --- |
| | | | |

### 调试记录
- 日期：
- 环境：
- 测试账号权限：
- 已验证页数/数据量：
- 登录失效表现：
- 限流表现：
- 待确认事项：
````

严禁把真实 Cookie、Token、Secret、手机号或客户数据写入文档。

## 十、最低验证清单

### 登录与账号

- 未登录时不能保存空凭证。
- 有普通 Cookie 但未登录时不能误判为有效。
- 有效凭证探测为 `active`。
- 过期凭证探测为 `expired`，同步返回明确提示。
- 私有账号不能被其他用户修改；共享账号权限符合预期。
- 捕获 token 过期、重复使用、跨用户使用均失败。

### 数据同步

- `table_meta` 字段 ID、字段名唯一，且只有一个主键。
- 第一页、最后一页、空列表、未知总数都正确。
- 多页没有重复主键、漏页或死循环。
- 近 3/7/30 天在新一轮同步时会随日期移动，同一轮分页保持时间不变。
- 固定日期不会被误当成相对时间。
- 凭证过期、限流、超时、上游 5xx、非 JSON 和超大响应都有明确行为。

### 安全与上线

- 管理 API 不能绕过可信网关直连。
- 非配置台 Origin 不能调用管理写接口。
- 第三方 API 请求不能访问白名单以外地址。
- 日志、任务表和响应中没有明文凭证。
- `/healthz`、`/readyz` 正常。
- 前端生产构建通过，后端测试与语法检查通过。

验证命令：

```bash
cd feishu-doudian-connector-server
npm test
npm run check

cd ../data-sync-fe-vue-demo
npm run build
```

## 十一、完成标准

满足下面条件后，才算从本模板完成一个新平台连接器：

1. 页面和错误信息中不再出现错误的平台名称。
2. 登录入口、凭证捕获、白名单、状态探测、失效识别均使用新平台规则。
3. 接口注册表、模块前缀和请求函数不再依赖抖店固定值。
4. 飞书身份隔离、签名、加密和安全限制仍然有效。
5. 第三方接口请求、返回、错误和调试记录已经归档。
6. 真实账号完成登录、配置、首轮同步、自动同步和凭证过期验证。

