# 接口配置速查表

这份文档只做一件事：

- 给后续维护的人提供可以直接复制的配置模板
- 顺手解释每个字段真正起什么作用

如果要看完整原理，请看：

- [数据同步插件接口选择到同步成功流程.md](/Users/ly/connector/feishu-doudian-connector/数据同步插件接口选择到同步成功流程.md)

---

## 一、最常见：GET 分页接口

适用于这类接口：

- `GET /xxx/list?page=0&pageSize=100`
- 返回里有列表和总数

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `method=GET`：参数拼到 URL 上
- `pageParam=page`：页码参数名叫 `page`
- `pageSizeParam=pageSize`：每页大小参数名叫 `pageSize`
- `pageStart=0`：第一页从 `0` 开始，不是 `1`
- `listPaths`：列表数据从哪里取
- `totalPaths`：总数字段从哪里取

最终真实请求长这样：

```text
GET /xxx/list?page=0&pageSize=100
```

---

## 二、GET 分页接口，页大小参数叫 `size`

适用于：

- `GET /xxx/list?page=0&size=100`

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "size",
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "size",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

重点只有一个：

- `pageSizeParam` 改成 `size`

---

## 三、POST 分页接口

适用于：

- `POST /xxx/list`
- 分页参数在请求体里

可直接复制：

```json
{
  "method": "POST",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page_no",
  "pageStart": 1,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "page_size",
  "requiredParams": [
    {
      "name": "page_no",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 1
    },
    {
      "name": "page_size",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `method=POST`：参数放请求体里，不拼到 URL
- `pageStart=1`：第一页是 `1`
- `pageParam=page_no`：页码字段名叫 `page_no`
- `pageSizeParam=page_size`：每页大小字段名叫 `page_size`

最终真实请求体类似：

```json
{
  "page_no": 1,
  "page_size": 100
}
```

---

## 三点五、POST 嵌套分页对象

适用于：

- `POST /xxx/list`
- 分页参数不是平铺字段
- 请求体要求长这样：

```json
{
  "page": {
    "current": 1,
    "page_size": 10
  }
}
```

可直接复制：

```json
{
  "method": "POST",
  "pageSize": 10,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page.current",
  "pageStart": 1,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "page.page_size",
  "requiredParams": [
    {
      "name": "page.current",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 1
    },
    {
      "name": "page.page_size",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 10
    }
  ]
}
```

怎么理解：

- `pageParam=page.current`：后端会写成 `body.page.current`
- `pageSizeParam=page.page_size`：后端会写成 `body.page.page_size`
- 这个写法用于 JSON POST 请求体
- 如果还需要固定查询条件，继续写在 `extraBody` 里

最终真实请求体类似：

```json
{
  "page": {
    "current": 1,
    "page_size": 10
  }
}
```

注意：

- 以前写 `pageParam: "page"` 会继续生成平铺字段 `"page": 1`
- 只有写成 `page.xxx` 这种点分路径时，才会生成嵌套对象

---

## 四、不分页接口

适用于：

- 一次请求就能查全量
- 接口没有 `page / size`

可直接复制：

```json
{
  "method": "GET",
  "pagination": false,
  "extraBody": {},
  "extraQuery": {},
  "listPaths": ["data"],
  "totalPaths": [],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false
}
```

怎么理解：

- `pagination=false`：后端不要自动传分页参数
- 飞书这边也只拉一次
- `hasMore=false`

注意：

- 不写 `pagination` 时，默认按 `true` 处理

---

## 五、带同步时间范围的接口

适用于：

- 页面上选“近 7 天”
- 后端自动帮你算开始和结束时间

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "dateRangeMapping": {
    "startTime": "audit_start_time",
    "endTime": "audit_end_time",
    "format": "date"
  },
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- 页面选“7天”时，后端会自动算出：
  - `audit_start_time`
  - `audit_end_time`
- `format=date` 表示传：
  - `2026-07-02`

还支持：

- `datetime`
- `timestamp_ms`
- `timestamp_s`

如果页面选的是“全量数据”，就不会传这两个字段。

---

## 六、带自定义查询字段的接口

适用于：

- 每个接口需要用户自己补不同的查询参数

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "customQueryFields": [
    {
      "name": "status",
      "label": "状态",
      "type": "string",
      "required": true,
      "placeholder": "请选择状态",
      "options": [
        {
          "value": "all",
          "label": "全部"
        },
        {
          "value": "active",
          "label": "启用"
        },
        {
          "value": "disabled",
          "label": "停用"
        }
      ]
    },
    {
      "name": "created_at_range",
      "label": "创建时间范围",
      "type": "dateRange",
      "startTime": "start_time",
      "endTime": "end_time",
      "format": "datetime_minute",
      "maxPastMonths": 6,
      "endPastMonths": 6,
      "required": true
    },
    {
      "name": "is_open",
      "label": "是否开启",
      "type": "boolean",
      "required": false
    }
  ],
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `customQueryFields` 不是直接请求参数
- 它是页面动态表单定义
- 用户填写后，值才会进入真实请求
- `required` 默认是 `false`，不写就是非必填
- 只有写 `required:true` 时，页面保存会要求用户填写

常用类型：

- `string`
- `integer`
- `number`
- `boolean`
- `dateRange`

`dateRange` 配置：

- `startTime` / `endTime`：真实请求里的开始/结束参数名
- `format`：`date`、`datetime`、`datetime_minute`、`timestamp_ms`、`timestamp_s`
- `maxPastMonths` / `maxPastDays`：可选窗口长度
- `endPastMonths` / `endPastDays`：可选窗口的截止日期距离今天多远，不填时截止到今天

最小 demo：

```json
{
  "name": "order_time_range",
  "label": "订单时间",
  "type": "dateRange",
  "startTime": "start_time",
  "endTime": "end_time",
  "format": "datetime",
  "maxPastMonths": 6,
  "endPastMonths": 6,
  "required": true
}
```

如果今天是 `2026-07-17`，这个配置只能选择 `2025-07-17` 到 `2026-01-17`，`2026-01-18` 之后会禁用。

用户选择 `2025-12-01` 到 `2025-12-02` 后，请求里会追加：

```json
{
  "start_time": "2025-12-01 00:00:00",
  "end_time": "2025-12-02 23:59:59"
}
```

字段说明：

| 字段 | 必填 | 说明 |
|---|---|---|
| `name` | 是 | 前端保存这个日期范围控件状态用的内部 key，不能和其他自定义字段重复。 |
| `label` | 否 | 页面表单展示名，不填时展示 `name`。 |
| `type` | 是 | 固定写 `dateRange`，表示渲染日期范围选择器。 |
| `startTime` | 是 | 用户选择的开始日期最终写入请求的参数名。 |
| `endTime` | 是 | 用户选择的结束日期最终写入请求的参数名。 |
| `format` | 否 | 输出格式，默认 `date`。可选 `date`、`datetime`、`datetime_minute`、`timestamp_ms`、`timestamp_s`。不是 `date` 时会显示时间选择。 |
| `maxPastDays` | 否 | 可选窗口长度，单位天。 |
| `maxPastMonths` | 否 | 可选窗口长度，单位月。和 `maxPastDays` 同时配置时，优先用 `maxPastDays`。 |
| `endPastDays` | 否 | 可选窗口截止日期距离今天的天数。 |
| `endPastMonths` | 否 | 可选窗口截止日期距离今天的月份数。和 `endPastDays` 同时配置时，优先用 `endPastDays`。 |
| `required` | 否 | 是否必填，默认 `false`。 |
| `helpText` | 否 | 表单下方的提示文案。 |

普通下拉 Select 这样写：

```json
{
  "name": "status",
  "label": "状态",
  "type": "string",
  "required": true,
  "placeholder": "请选择状态",
  "options": [
    {
      "value": "all",
      "label": "全部"
    },
    {
      "value": "active",
      "label": "启用"
    },
    {
      "value": "disabled",
      "label": "停用"
    }
  ]
}
```

怎么理解：

- `options` 有值时，前端会渲染成下拉框
- `value` 是最终传给接口的参数值
- `label` 是页面上给用户看的文案
- `type` 一般写 `string`，除非接口明确要求数字或布尔值
- `required` 不写时默认非必填；必填下拉框要写 `required:true`

---

## 七、需要固定 Query 参数的接口

适用于：

- 成功请求里本来就要固定带某些参数

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {
    "scene": "product",
    "status": "all"
  },
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `extraQuery` 里的参数会固定带上
- 适合写那些每次都一样的参数

---

## 八、需要固定 Body 参数的 POST 接口

适用于：

- POST 请求里除了分页，还有固定 body 字段

可直接复制：

```json
{
  "method": "POST",
  "pageSize": 100,
  "extraBody": {
    "scene": "shop_list",
    "status": 1
  },
  "listPaths": ["data.list"],
  "pageParam": "page_no",
  "pageStart": 1,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "page_size",
  "requiredParams": [
    {
      "name": "page_no",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 1
    },
    {
      "name": "page_size",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `extraBody` 会并到 POST 请求体里
- 适合固定过滤条件

---

## 九、需要特殊请求头的接口

适用于：

- 某些接口需要固定 `Referer`
- 或需要补 `X-Requested-With`

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data.list"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "referer": "https://fxg.jinritemai.com/ffa/g/list",
  "extraHeaders": {
    "X-Requested-With": "XMLHttpRequest"
  },
  "requiredParams": [
    {
      "name": "page",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 0
    },
    {
      "name": "pageSize",
      "type": "integer",
      "source": "pagination",
      "required": true,
      "defaultValue": 100
    }
  ]
}
```

怎么理解：

- `referer`：指定来源页面
- `extraHeaders`：额外请求头

一般只有这类情况才需要：

- ApiPost 成功，代码失败
- 参数和 Cookie 看起来都对
- 怀疑是请求上下文差异

---

## 九点五、成功码不是 `0` 的接口

适用于：

- 控制台里已经能看到 `data`
- 但接口 `code` 不是 `0`
- 例如返回 `code=100000, msg=success`

当前后端默认兼容的成功码有：

- `0`
- `200`
- `100000`

如果某个接口有自己特殊的成功码，可以在数据库 `request_config` 里显式配置：

```json
{
  "successCodes": ["0", "200", "100000"]
}
```

怎么理解：

- 后端会按 `successCodes` 白名单判断这次响应是不是成功
- 如果不配置，就用默认成功码集合
- 这和 `parseListItemJson` 是两回事
- `parseListItemJson` 管的是“列表项是不是字符串 JSON”
- `successCodes` 管的是“当前响应该不该被当成成功”

---

## 十、列表项是 JSON 字符串的接口

适用于：

- `listPaths` 命中的数组里，每一项不是对象
- 而是类似 `"{\"order_id\":\"xxx\"}"` 这样的 JSON 字符串

可直接复制：

```json
{
  "method": "GET",
  "pageSize": 100,
  "extraBody": {},
  "listPaths": ["data"],
  "pageParam": "page",
  "pageStart": 0,
  "extraQuery": {},
  "totalPaths": ["data.total"],
  "contentType": "application/json;charset=UTF-8",
  "includeShopId": false,
  "pageSizeParam": "pageSize",
  "parseListItemJson": true
}
```

也可以写成：

```json
{
  "listPaths": ["data"],
  "listItemFormat": "json_string"
}
```

怎么理解：

- 默认不解析
- 只有显式配置了 `parseListItemJson=true`
- 或者 `listItemFormat=json_string`
- 后端才会把列表每一项先 `JSON.parse`

例如接口返回：

```json
[
  "{\"order_id\":\"6926380170248355402\",\"pay_type\":\"PA\"}"
]
```

开启后，后端会先转成：

```json
[
  {
    "order_id": "6926380170248355402",
    "pay_type": "PA"
  }
]
```

然后字段配置仍然正常写：

```json
{
  "key": "order_id",
  "sourcePath": "order_id"
}
```

如果不配置这个开关，`sourcePath` 是取不到字符串内部字段的。

---

## 十一、字段配置模板

### 1. 普通文本字段

```json
{
  "key": "product_name",
  "type": "Text",
  "label": "商品名称(product_name)",
  "fieldName": "商品名称",
  "isPrimary": false,
  "sourcePath": "product_name",
  "defaultField": "col_product_name"
}
```

### 2. 字典映射字段

适用于：

- 接口返回的是状态码、枚举值、渠道码
- 但飞书里希望展示成人能看懂的中文文本

可直接复制：

```json
{
  "key": "trade_type_desc",
  "type": "Text",
  "label": "交易类型(trade_type_desc)",
  "fieldName": "交易类型",
  "sourcePath": "trade_type",
  "defaultField": "col_trade_type_desc",
  "valueMap": {
    "1": "待结算",
    "2": "已结算",
    "3": "已退款"
  }
}
```

也支持下面两种别名写法：

```json
{
  "enumMap": {
    "PA": "聚合支付",
    "WX": "微信支付",
    "NEW_HZ": "抖音支付"
  }
}
```

```json
{
  "dict": {
    "0": "关闭",
    "1": "开启"
  }
}
```

如果你更喜欢数组格式，也支持：

```json
{
  "key": "pay_type",
  "type": "Text",
  "sourcePath": "pay_type",
  "valueMap": [
    { "value": "PA", "label": "聚合支付" },
    { "value": "WX", "label": "微信支付" },
    { "value": "NEW_HZ", "label": "抖音支付" }
  ]
}
```

怎么理解：

- 后端先按 `sourcePath` 取原始值
- 再按 `valueMap / enumMap / dict` 做字典映射
- 命中后写入飞书的是映射后的文本，不是原始码值

补充说明：

- 当前代码默认未命中也会回写原始值
- `enumFallback` / `valueMapFallback` 可以控制兜底策略
- 目前常用的兜底值可以写 `raw` 或 `empty`

例如：

```json
{
  "key": "status_text",
  "type": "Text",
  "sourcePath": "status",
  "valueMap": {
    "0": "失败",
    "1": "成功"
  },
  "enumFallback": "raw"
}
```

这一段非常适合：

- `pay_type`
- `status`
- `channel_type`
- `trade_type`
- 任何接口里返回代码、页面却想展示中文说明的字段

### 2. 数字字段

```json
{
  "key": "stock_num",
  "type": "Number",
  "label": "库存(stock_num)",
  "fieldName": "库存",
  "isPrimary": false,
  "sourcePath": "stock_num",
  "defaultField": "col_stock_num"
}
```

### 3. 价格字段

```json
{
  "key": "discount_price",
  "type": "price",
  "label": "折后价(discount_price)",
  "fieldName": "折后价",
  "isPrimary": false,
  "sourcePath": "discount_price",
  "defaultField": "col_discount_price"
}
```

怎么理解：

- `price` 默认按“分转元”
- 例如接口给 `1234`，飞书里会写成 `12.34`
- 如果原始值本来就是元，不要用 `price`

### 4. 日期字段

```json
{
  "key": "create_time",
  "type": "DateTime",
  "label": "创建时间(create_time)",
  "fieldName": "创建时间",
  "isPrimary": false,
  "sourcePath": "create_time",
  "defaultField": "col_create_time"
}
```

### 5. 数组路径字段

只取数组第一项时，用固定下标 `[0]`：

```json
{
  "key": "reduce_price",
  "type": "price",
  "label": "降价建议(reduce_price)",
  "fieldName": "降价建议",
  "isPrimary": false,
  "sourcePath": "suggest_advice_info.advice_info[0].reduce_price",
  "defaultField": "col_reduce_price"
}
```

怎么理解：

- `sourcePath` 支持 `[0]`
- 可以读取数组里的第一项

读取数组里的所有项时，用通配符 `[*]` 或 `[]`：

```json
{
  "key": "orderList_productName",
  "type": "Text",
  "label": "商品信息",
  "fieldName": "商品信息",
  "isPrimary": false,
  "sourcePath": "orderList[*].skuOrderInfoList[*].productName",
  "defaultField": "col_product_name"
}
```

```json
{
  "key": "payAmount",
  "type": "price",
  "label": "支付金额",
  "fieldName": "支付金额",
  "isPrimary": false,
  "sourcePath": "orderList[*].skuOrderInfoList[*].payAmount",
  "defaultField": "col_payAmount"
}
```

通配数组字段的写入规则：

- `Text`：多个值用 `、` 拼接，例如多个商品名写到同一个单元格
- `price`：多个值会先按“分转元”，再求和
- `Number`：多个值默认求和
- `DateTime` / 链接字段：默认取第一个有效值
- 如只想取第一个值，可以额外配置 `"arrayMode": "first"`
- 如想保留原始数组，可以额外配置 `"arrayMode": "json"`
- 如想自定义文本分隔符，可以配置 `"arrayJoiner": "\n"` 或其他分隔符

### 6. 主键字段

```json
{
  "key": "product_id",
  "type": "Text",
  "label": "商品 ID(product_id)",
  "fieldName": "商品 ID",
  "isPrimary": true,
  "sourcePath": "product_id",
  "defaultField": "col_product_id"
}
```

注意：

- 一个接口只能有一个 `isPrimary=true`
- 不要把 `start_time`、`end_time` 这种重复值字段设成主键

---

## 十二、最容易搞错的 8 件事

### 1. 只改了 `requiredParams`，没改 `pageParam`

错误想法：

```json
{
  "requiredParams": [
    { "name": "page_no" }
  ]
}
```

这不会让后端自动发 `page_no`。

必须改这里：

```json
{
  "pageParam": "page_no"
}
```

### 2. `GET` 和 `POST` 不只是换一个单词

- `GET`：参数在 URL
- `POST`：参数在 Body

### 3. `pageStart` 写错

- 有的接口第一页是 `0`
- 有的接口第一页是 `1`

### 4. `listPaths` 写错

例如响应是：

```json
{
  "data": {
    "items": []
  }
}
```

那就该写：

```json
["data.items"]
```

不是 `["data"]`

### 5. 不分页接口没写 `pagination:false`

结果后端还在自动传 `page/pageSize`，接口直接报错。

### 6. `price` 用错

- 原始值是“分” -> 用 `price`
- 原始值已经是“元” -> 用 `Number`

### 7. 主键配了两个

飞书只允许一个主键字段。

### 8. 页面还是旧字段

改完数据库后，要点页面上的：

- `刷新接口配置`

---

## 十三、排查顺序

接口配完不工作时，按这个顺序查最省时间：

1. `method` 对不对
2. `pageParam` / `pageSizeParam` 对不对
3. `pageStart` 是 `0` 还是 `1`
4. 是否其实应该 `pagination:false`
5. `listPaths` / `totalPaths` 对不对
6. 是否需要 `dateRangeMapping`
7. 是否需要 `customQueryFields`
8. 是否需要 `extraQuery` / `extraBody`
9. 是否需要 `referer` / `extraHeaders`
10. `sourcePath` 是否写错
11. `isPrimary` 是否只保留一个

---

## 十四、建议

新增接口时，最稳的做法是：

1. 先在 ApiPost 或浏览器里确认成功请求长什么样
2. 再按上面的模板选最近的一份复制
3. 只改必要字段
4. 改完后点击页面上的 `刷新接口配置`
5. 先点一次 `测试连接`

如果你发现某个接口很特殊，不要硬凑模板，直接在完整文档里补规则。
