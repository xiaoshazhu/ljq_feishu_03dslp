# 飞书抖店连接器生产化整改记录

> 整改日期：2026-07-14
>
> 适用目录：`feishu-doudian-connector-server`、`data-sync-fe-vue-demo`
>
> 飞书协议参考：[飞书多维表格数据连接器开发文档](https://feishu.feishu.cn/docx/GwBXdq7cmoZahfx8faQchcS2nXf)
>
> 该链接是飞书官方外部指南，当前仅有阅读/评论权限；项目问题与修复明细以本文档为准。

## 1. 当前结论

本轮已完成后端安全、稳定性、多租户隔离、凭证捕获、并发背压、超时、分页、同步日志、数据库迁移和前端交互等生产化整改。

代码级验证已通过，但在正式公网发布前仍有三个必须由部署方完成的条件：

1. 部署可信认证网关：先验证真实用户会话，再按第 4 节规范签名管理 API；后端必须只允许网关或可信内网访问。
2. 轮换历史中出现过的抖店 Cookie、MySQL 密码、飞书 `SECRET_KEY`，并生成相互独立的 `MANAGEMENT_IDENTITY_SECRET`、`CREDENTIAL_ENCRYPTION_KEY` 与 `LOCAL_AGGREGATE_TOKEN`。
3. 按 `.env.example` 修正真实生产环境配置。2026-07-14 本机只读检查当前 `.env` 时首先因缺少 `SECRET_KEY` 被启动校验拒绝；补齐后仍需继续通过其余变量校验。

在上述条件完成前，项目适合继续联调、预发布和内网验收，不应直接暴露为无人值守的公网生产服务。

## 2. 整改问题清单

### 2.1 安全与凭证

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| SEC-01 | P0 | MySQL 地址、账号或密码曾以内置默认值存在，源码泄露即可直接访问数据库 | 移除真实默认值，生产启动时强制从环境变量读取 | `runtime_config.js`、`database.js`、`.env.example` | 已修复 |
| SEC-02 | P0 | 运行时 `tasks.json`、`errors.json` 曾被 Git 跟踪，其中任务配置可能携带 Cookie | 删除被跟踪的运行数据，加入忽略规则，仅保留脱敏 example | `.gitignore`、`data/*.example.json` | 已修复 |
| SEC-03 | P0 | 抖店 Cookie 在 MySQL 中以明文保存 | 使用 AES-256-GCM 加密，随机 IV、认证标签防篡改，并兼容历史明文读取和分批迁移 | `credential_cipher.js`、`database.js` | 已修复 |
| SEC-04 | P0 | 账号和捕获状态 API 可能把 Cookie 返回浏览器 | 公共账号 DTO 和捕获状态响应明确剔除 Cookie，任务配置保存前再次删除敏感字段 | `index.js`、`App.vue` | 已修复 |
| SEC-05 | P0 | 旧书签载荷直接携带租户和用户 ID，攻击者可伪造分区写入凭证 | 改为 32 字节短效一次性 token，数据库只保存 SHA-256，token 与用户上下文在服务端绑定并原子消费 | `database.js`、`index.js`、`App.vue` | 已修复 |
| SEC-06 | P1 | 在 URL 中携带捕获令牌会进入历史、代理日志和 Referer；抖店 CSP 又会阻止直接跨域上报 | 新增严格 CSP 的同源中转页，通过受限 `postMessage` 传递 token 和 Cookie，URL 不承载敏感值 | `index.js`、`App.vue` | 已修复 |
| SEC-07 | P1 | Cookie 捕获端点的跨域来源过宽 | 只允许 `DOUDIAN_CAPTURE_ALLOWED_ORIGINS`，拒绝缺失或未授权 Origin，预检同样校验 | `index.js`、`test/index_security.test.js` | 已修复 |
| SEC-08 | P0 | 飞书签名若基于重新序列化 JSON，字段顺序或空白变化会导致误判，也可能偏离官方签名输入 | Express 保存原始 Body 字节，使用 SHA-1 和常量时间比较完成验签 | `request_sign.js`、`index.js`、`test/request_sign.test.js` | 已修复 |
| SEC-09 | P0 | 数据库可配置上游 Host/Path，错误或恶意配置可能形成 SSRF | 主动请求仅允许显式 HTTPS Origin 白名单；本地聚合默认只允许回环地址 | `http_client.js`、`runtime_config.js`、`dy_helper.js` | 已修复 |
| SEC-10 | P0 | 真实请求失败时可能静默降级到 Mock，造成生产数据伪造 | Mock 仅在非生产且显式开启时可用，生产环境启动时禁止 `ALLOW_MOCK_DATA=true` | `table_records.js`、`runtime_config.js` | 已修复 |
| SEC-11 | P1 | 本地聚合内部接口可能复用飞书签名密钥 | 生产环境强制配置独立且至少 32 字符的 `LOCAL_AGGREGATE_TOKEN` | `runtime_config.js`、`doudian_local_aggregate.js` | 已修复 |
| SEC-12 | P1 | 调试日志可能输出完整 Cookie、带 token 的 URL、聚合主体参数、请求正文或未知响应片段 | 请求头脱敏、URL 去除 Query，不记录响应正文和聚合主体明细，仅保留正文 key、字节数、来源数量、Cookie 长度和请求 ID | `http_client.js`、`dy_helper.js`、`doudian_aggregate_runner.js`、`doudian_local_aggregate.js`、`index.js` | 已修复 |
| SEC-13 | P1 | API 缺少请求体上限、基础安全响应头和生产 HSTS | 增加 JSON Body 限制、`nosniff`、Referrer Policy、Permissions Policy、HSTS 和敏感 API `no-store` | `index.js` | 已修复 |
| SEC-14 | P1 | 抖店接口详情包含请求配置，旧路由可被任意来源读取 | 接口目录和详情增加生产同源校验与管理限流 | `index.js` | 已修复 |
| SEC-15 | P0 | 管理 API 仅靠前端传入的飞书用户 ID，攻击者可伪造身份访问其他租户或用户数据 | 新增可信网关 HMAC-SHA256 认证，签名绑定时间戳、nonce、方法、完整转发路径、原始正文哈希、企业和用户；Body/Query 中伪造身份不再生效 | `management_identity.js`、`index.js`、`runtime_config.js` | 已修复，待部署网关 |
| SEC-16 | P1 | 多实例只做内存防重放会被跨实例绕过；按接收时间清理 nonce 又会在未来时间偏差场景留下短暂重放窗口 | 新增 MySQL nonce 哈希唯一表，原子消费并按“签名时间戳 + 有效窗口”过期，完整覆盖签名剩余有效期 | `database.js`、`index.js`、`test/management_identity_http.test.js` | 已修复 |

### 2.2 多租户与权限

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| TEN-01 | P0 | 账号、任务、异常和日志查询缺少统一企业分区 | 所有核心表和查询统一使用 `company_id`，输入先做长度和字符校验 | `database.js`、`index.js` | 已修复 |
| TEN-02 | P0 | 企业共享账号与个人私有账号权限边界不完整 | 私有账号仅所有者可见和修改；共享账号可使用但不能被非所有者删除或改写凭证 | `database.js`、`index.js` | 已修复 |
| TEN-03 | P1 | `accounts.is_active` 是账号全局状态，不同用户选择共享账号时会互相覆盖 | 新增 `account_selections`，按企业和用户保存当前活跃账号，并迁移旧选择 | `database.js` | 已修复 |
| TEN-04 | P1 | 账号 key 可能碰撞，旧逻辑还会用店铺名等模糊匹配错误账号 | 新账号使用 UUID；保存时校验企业内 key 所有权；同步时配置存在 key 就只做精确匹配 | `App.vue`、`database.js`、`table_records.js` | 已修复 |
| TEN-05 | P1 | 生产请求缺失租户或用户时会落入共享 `default` 分区 | 生产管理 API 只采用验签后的 `authenticatedIdentity`，并拒绝 `default`、`unknown` 身份上下文 | `index.js` | 已修复 |

### 2.3 高并发、超时与资源保护

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| REL-01 | P0 | 原生 fetch 没有真实超时，网络卡死会长期占用请求和账号槽位 | 使用 AbortController 主动取消上游请求 | `http_client.js`、`test/http_client.test.js` | 已修复 |
| REL-02 | P0 | 原超时只覆盖响应头，慢速或超大响应体仍可长期占连接并耗尽内存 | 请求头和响应体共享同一截止时间；流式读取前检查 10 MB 可配置上限 | `http_client.js`、`dy_helper.js`、聚合模块 | 已修复 |
| REL-03 | P1 | 上游 HTTP 非 2xx 仍可能继续按成功 JSON 解析 | 所有真实抖店和聚合请求先检查 HTTP 状态，再检查业务 code | `dy_helper.js`、`doudian_aggregate_runner.js`、`doudian_local_aggregate.js` | 已修复 |
| REL-04 | P0 | 飞书 `table_meta` 10 秒、`records` 20 秒限制附近容易超时 | 设置 8 秒和 18 秒内部截止时间，延迟和后续上游请求都按剩余预算提前失败 | `index.js`、`http_client.js`、`dy_helper.js` | 已修复 |
| REL-05 | P0 | 同步请求无限并发会压垮抖店账号、Node 进程和 MySQL | 增加实例、企业、账号三级 Semaphore、有限队列、等待超时和可重试忙碌响应 | `concurrency_control.js`、`index.js` | 已修复 |
| REL-06 | P1 | 热点账号排队请求会提前占住企业和全局槽位，其他账号被无辜阻塞 | 改为账号、企业、全局顺序获取槽位，热点队列不再吞掉共享容量 | `concurrency_control.js`、`test/concurrency_control.test.js` | 已修复 |
| REL-07 | P1 | 管理和捕获接口缺少限流；无限来源又可能让限流 Map 自身耗尽内存 | 增加固定窗口限流、桶数量上限、清理和 `Retry-After` | `rate_limiter.js`、`index.js` | 已修复 |
| REL-08 | P1 | MySQL 连接池上限、空闲连接和排队容量缺少一致约束 | 显式配置连接上限、`maxIdle`、连接/空闲超时和队列上限，保证空闲数不超过总连接数 | `runtime_config.js` | 已修复 |
| REL-09 | P0 | 多实例同时启动可能并发改表；旧代码还会在启动时静默删旧表 | 使用 MySQL advisory lock 串行迁移，禁止自动删表，不兼容 Schema 直接阻止启动 | `database.js` | 已修复 |
| REL-10 | P1 | 旧表存在但 `cookie`/`module` 缺列或长度不足时迁移会失败或截断 | 缺列时新增；Cookie 升级到 MEDIUMTEXT；模块字段至少 255 字符 | `database.js` | 已修复 |
| REL-11 | P1 | 服务缺少就绪探针和优雅停机，发布时可能接收未就绪流量或中断在途请求 | 增加 `/healthz`、`/readyz`、SIGTERM/SIGINT 停机、连接排空和 MySQL 关闭 | `index.js`、`database.js` | 已修复 |
| REL-12 | P2 | 大响应未压缩、Socket 无明确生命周期 | 启用压缩、Keep-Alive、Headers/Request Timeout 和每 Socket 最大请求数 | `index.js` | 已修复 |
| REL-13 | P1 | 缺少可重复的突发负载回归，无法持续验证背压是否会突破并发上限或留下悬挂请求 | 新增 100 请求并发测试，断言全局执行上限、过载 `SYNC_BUSY` 可重试响应和全部请求最终释放 | `test/concurrency_control.test.js` | 已修复 |

### 2.4 数据正确性与可观测性

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| DATA-01 | P0 | 上游不返回 `total` 时，旧逻辑可能把第一页误判为最后一页 | 优先使用 `hasMore`；未知总数时按“本页是否达到 pageSize”继续分页 | `dy_helper.js`、`table_records.js` | 已修复 |
| DATA-02 | P1 | 每次重试生成新日志 key，会把同一事务页记成多次独立执行 | 日志 key 由事务和页 token 稳定生成；每页独立记录开始、成功、失败、耗时和下一页 | `index.js`、`database.js` | 已修复 |
| DATA-03 | P1 | 超长 key、状态、错误消息和页码参数可能触发 MySQL 截断或异常 | 所有持久化字段按 Schema 归一化；分页范围、记录数、耗时和状态枚举均有限制 | `database.js` | 已修复 |
| DATA-04 | P0 | 飞书任务配置长期保存旧 Cookie，账号重新绑定后同步仍使用过期凭证 | 每次 `records` 前按企业、用户和账号 key 从 MySQL 刷新最新凭证 | `table_records.js` | 已修复 |
| DATA-05 | P1 | 捕获缓冲可被并发重复消费，失败时还可能丢失凭证 | 使用事务和 `FOR UPDATE` 原子消费；账号保存失败时恢复捕获缓冲 | `database.js`、`index.js` | 已修复 |
| DATA-06 | P1 | 错误响应结构不稳定，前端只能展示模糊报错 | 引入 `AppError`、稳定 `errorCode`、HTTP 状态、`retryable` 和请求 ID | `app_error.js`、`index.js`、`App.vue` | 已修复 |
| DATA-07 | P2 | 默认单页过小会增加飞书分页请求数和抖店调用次数 | 默认 `MAX_SYNC_PAGE_SIZE` 调整为 200，同时保持上限保护 | `runtime_config.js`、`table_records.js` | 已修复 |

### 2.5 前端与构建

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| FE-01 | P0 | 书签脚本长期复用身份参数和令牌，过期后提示不清晰 | 改为一次性 token，临近过期自动刷新，失败明确提示重新生成 | `App.vue` | 已修复 |
| FE-02 | P1 | fetch 只看网络成功，不识别业务非 0 code，用户看不到请求 ID | 统一 `parseApiResponse`，同时识别 HTTP 和业务错误并展示 request ID | `App.vue` | 已修复 |
| FE-03 | P1 | 使用时间戳生成账号 key，高并发或快速点击可能碰撞 | 使用 `crypto.randomUUID()`，提供兼容兜底 | `App.vue` | 已修复 |
| FE-04 | P2 | Ant Design Vue 全量注册导致主业务包过大 | 改为组件按需注册并拆分 Vue、Ant Design 和飞书 SDK vendor chunk | `main.ts`、`vite.config.ts` | 已修复 |
| FE-05 | P2 | 生产构建缺失时后端仍可能以残缺页面启动 | 生产启动前强制检查 `dist/index.html` | `index.js` | 已修复 |

### 2.6 工程与供应链

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| OPS-01 | P1 | 缺少可复制的生产配置模板，配置错误只能在运行中发现 | 增加完整 `.env.example` 和启动前安全校验 | `.env.example`、`runtime_config.js` | 已修复 |
| OPS-02 | P1 | 缺少自动化回归，安全和并发修改容易反复 | 新增 Node Test，覆盖并发、凭证加密、超时、响应体上限、SSRF、限流、验签和生产配置 | `test/*.test.js` | 已修复 |
| OPS-03 | P1 | 依赖漏洞状态不明 | 2026-07-14 使用 npm 官方 Registry 审计，前后端均为 0 个已知漏洞 | 两个 `package-lock.json` | 已验证 |
| OPS-04 | P0 | Git 历史中存在疑似真实 Cookie 和硬编码凭证回退值 | 当前工作树已清理；必须轮换相关凭证，并在备份后使用专业工具清理远端历史 | Git 历史 | 待运维处理 |
| OPS-05 | P2 | 开发规范引用了旧电脑的绝对 `file://` 路径，换机器后数据库文档链接失效 | 改为仓库内相对链接，并加入 Markdown 本地链接扫描 | `开发标准.md` | 已修复 |

### 2.7 现有功能闭环

本节记录 2026-07-14 在“暂不考虑部署与压测，只检查现有功能是否正常”的范围内完成的逐项修复。

| 编号 | 级别 | 问题与根因 | 修复内容 | 主要文件 | 状态 |
| --- | --- | --- | --- | --- | --- |
| FUNC-01 | P1 | 编辑账号时允许把 Cookie 更新为空，可能直接清除有效凭证 | 数据库更新层拒绝空 Cookie，保留原凭证并返回稳定校验错误 | `database.js` | 已修复 |
| FUNC-02 | P1 | 手工粘贴 Cookie 无法解析店铺 ID 时，前端可能把“手动录入”等展示文本当作真实 ID | 新增和编辑账号都只接受捕获 ID、Cookie 中的数字 ID 或页面填写的数字 Shop ID，无法确定时阻止保存 | `App.vue` | 已修复 |
| FUNC-03 | P1 | URL 中 `isNew=false` 被 `Boolean("false")` 判定为新建模式，已有飞书配置无法恢复 | 新建模式只接受 `1`、`true`、`yes`，其他值均按已有配置恢复 | `App.vue` | 已修复 |
| FUNC-04 | P1 | “测试连接”没有携带页面选择的同步时间范围，测试请求与正式同步请求不一致 | 前端传递 `dateRange`，后端测试路由继续透传给真实请求构造 | `App.vue`、`index.js` | 已修复 |
| FUNC-05 | P1 | 测试连接和配置保存没有统一检查动态必填 Query 参数 | 两个入口均调用 `validateRequiredCustomQueryFields()`，缺值时直接定位并提示对应字段 | `App.vue` | 已修复 |
| FUNC-06 | P1 | Shop ID 只检查非空，中文、空格或其他非数字内容会进入真实抖店请求 | 前端测试与保存统一校验纯数字，后端测试接口再次校验最终解析后的 Shop ID | `App.vue`、`index.js` | 已修复 |
| FUNC-07 | P0 | 用户可以取消主键字段，导致飞书 `table_meta` 返回无主键结构 | 前端主键不可取消和清空；清空字段时保留主键；后端 `table_meta` 与 `records` 均严格要求恰好一个主键 | `App.vue`、`table_meta.js`、`table_records.js`、`index.js` | 已修复 |
| FUNC-08 | P0 | 缺失主键值时使用页码和行号生成临时 ID，跨页或重试后不稳定 | 缺失主键直接返回 `DoudianPrimaryValueMissing`；数值 `0` 保持为合法主键 | `table_records.js` | 已修复 |
| FUNC-09 | P1 | 同一页出现重复主键时没有明确错误，飞书可能覆盖错误记录 | 写入前检查本页 `primaryID` 唯一性，重复时返回 `DoudianPrimaryValueDuplicate` | `table_records.js` | 已修复 |
| FUNC-10 | P0 | 多个源字段映射到同一个目标列时会静默覆盖前一个值 | 前端保存前检查目标字段 ID；`table_meta` 检查字段 ID/名称非空且唯一；`records` 写入时再次防御 | `App.vue`、`table_meta.js`、`table_records.js` | 已修复 |
| FUNC-11 | P1 | 业务字段可占用连接器公共列 `account_name`，导致账号名称覆盖业务值 | 前端拒绝该映射，表结构拒绝接口 Schema 占用保留列，记录写入层再次检查冲突 | `App.vue`、`table_meta.js`、`table_records.js` | 已修复 |
| FUNC-12 | P0 | 路由层会再次过滤 `table_meta` 字段并可能删除真实主键，再把第一列伪装成主键 | 移除重复过滤和主键伪造，路由只做协议字段名归一化并严格验证一个主键 | `index.js` | 已修复 |
| FUNC-13 | P1 | Form 请求遇到 `page.current` 等嵌套参数会编码成 `[object Object]` | GET Query 和 `application/x-www-form-urlencoded` Body 统一递归展开为点分参数 | `dy_helper.js` | 已修复 |
| FUNC-14 | P1 | GET 接口即使配置 `includeOriginHeader=true` 仍会删除 `Origin` | GET 和非 GET 均按接口配置决定是否携带 `Origin` | `dy_helper.js` | 已修复 |
| FUNC-15 | P1 | 本地聚合请求仍使用旧的嵌套对象编码方式 | 聚合 GET/Form 请求使用与普通接口一致的点分展开规则 | `doudian_aggregate_runner.js` | 已修复 |
| FUNC-16 | P0 | 非法或越界分页 token 可能静默回到第一页或被误判为同步结束，造成重复、漏数或循环 | 普通分页严格校验 token；聚合 token 校验格式、来源下标、页码和偏移；仍有后续数据却缺少 token 时直接失败 | `table_records.js`、`doudian_aggregate_runner.js`、`doudian_local_aggregate.js` | 已修复 |
| FUNC-17 | P1 | `rolling` 时间范围每页按当前时间重新计算，跨页时边界漂移，可能重复或遗漏临界数据 | 首屏生成毫秒时间锚点并写入后续分页 token，普通和聚合分页均复用同一时间窗口 | `dy_helper.js`、`doudian_aggregate_runner.js` | 已修复 |
| FUNC-18 | P1 | 短 Cookie 的日志脱敏逻辑会原样输出完整值 | 短值全部遮挡；长值只保留首尾各 4 个字符和长度 | `doudian_aggregate_runner.js` | 已修复 |
| FUNC-19 | P1 | 旧配置把主键映射保存为空时，记录层可能跳过主键数据列 | 主键映射无论旧配置是否为空都回退到默认字段 ID，前端同时禁止清空主键映射 | `App.vue`、`table_records.js` | 已修复 |
| FUNC-20 | P1 | 普通分页页码缺少上限，且恶意或误配的点分参数路径可能写入对象原型 | 普通页码和累计条数增加安全范围；普通与聚合分页拒绝 `__proto__`、`prototype`、`constructor` 路径 | `table_records.js`、`dy_helper.js`、`doudian_aggregate_runner.js` | 已修复 |

## 3. 生产环境变量

以 `feishu-doudian-connector-server/.env.example` 为唯一配置模板，不提交真实 `.env`。

### 3.1 基础与 MySQL

| 变量 | 默认/示例 | 用途 |
| --- | --- | --- |
| `NODE_ENV` | `production` | 开启生产安全校验、静态前端和 HSTS |
| `PORT` | `3000` | Node 服务监听端口 |
| `FRONTEND_PUBLIC_URL` | `https://connector.example.com` | 对外 HTTPS 地址及管理 API 同源基准 |
| `FRONTEND_DEV_SERVER` | `http://127.0.0.1:5173` | 非生产模式 Vite 代理地址 |
| `TRUST_PROXY_HOPS` | `1` | 可信反向代理层数，必须与真实拓扑一致 |
| `MYSQL_HOST`、`MYSQL_PORT` | 必填、`3306` | MySQL 地址 |
| `MYSQL_USER`、`MYSQL_PASSWORD`、`MYSQL_DATABASE` | 必填 | 独立最小权限数据库账号 |
| `MYSQL_CONNECTION_LIMIT` | `20` | 单实例最大 MySQL 连接数 |
| `MYSQL_MAX_IDLE_CONNECTIONS` | `20` | 最大空闲连接数，不会超过连接总数 |
| `MYSQL_QUEUE_LIMIT` | `500` | 连接池等待队列上限 |
| `MYSQL_CONNECT_TIMEOUT_MS` | `5000` | 建连超时 |
| `MYSQL_IDLE_TIMEOUT_MS` | `60000` | 空闲连接回收 |
| `MYSQL_HEALTH_QUERY_TIMEOUT_MS` | `2000` | 就绪探针 SQL 超时 |
| `MYSQL_MIGRATION_LOCK_TIMEOUT_MS` | `30000` | 多实例 Schema 锁等待时间 |

### 3.2 签名、凭证与来源

| 变量 | 默认/示例 | 用途 |
| --- | --- | --- |
| `MANAGEMENT_IDENTITY_AUTH_MODE` | 生产必须为 `gateway_hmac` | 开启管理 API 可信网关身份认证 |
| `MANAGEMENT_IDENTITY_SECRET` | 必填 | 网关与后端共享的独立 HMAC 密钥，至少 32 字符，禁止下发前端或复用其他密钥 |
| `MANAGEMENT_IDENTITY_MAX_AGE_MS` | `30000` | 管理身份签名最大时钟偏差和有效窗口 |
| `SECRET_KEY` | 必填 | 飞书数据连接器签名密钥 |
| `REQUIRE_FEISHU_SIGNATURE` | 生产为 `true` | 强制校验 `/api/table_meta` 和 `/api/records` |
| `FEISHU_SIGNATURE_MAX_AGE_MS` | `0` | 官方规范未强制时间窗口，默认关闭；启用前确保时钟同步 |
| `CREDENTIAL_ENCRYPTION_KEY` | 必填 | 32 字节随机值，支持 64 位 hex 或标准 Base64 |
| `LOCAL_AGGREGATE_TOKEN` | 必填 | 独立内部令牌，至少 32 字符，不得复用 `SECRET_KEY` |
| `LOCAL_AGGREGATE_BASE_URL` | `http://127.0.0.1:3000` | 本地聚合服务地址 |
| `ALLOW_REMOTE_LOCAL_AGGREGATE` | `false` | 只有独立可信内网聚合服务才开启 |
| `CREDENTIAL_MAX_BYTES` | `65535` | 单条 Cookie 最大 UTF-8 字节数 |
| `CAPTURE_SESSION_TTL_MS` | `900000` | 一次性捕获 token 有效期 |
| `DOUDIAN_CAPTURE_ALLOWED_ORIGINS` | 两个官方 Origin | 允许执行书签捕获的页面来源 |
| `DOUDIAN_ALLOWED_API_ORIGINS` | 两个官方 Origin | 后端主动请求白名单 |
| `ALLOW_MOCK_DATA` | `false` | 生产必须关闭 |

### 3.3 容量、超时与限流

| 变量 | 默认值 | 用途 |
| --- | --- | --- |
| `MAX_SYNC_PAGE_SIZE` | `200` | 飞书单页最大记录数 |
| `SYNC_GLOBAL_CONCURRENCY` | `40` | 单实例同步执行上限 |
| `SYNC_COMPANY_CONCURRENCY` | `10` | 单企业同步执行上限 |
| `SYNC_ACCOUNT_CONCURRENCY` | `1` | 单抖店账号并发，建议保持 1 |
| `SYNC_GLOBAL_QUEUE_LIMIT` | `200` | 单实例等待队列 |
| `SYNC_COMPANY_QUEUE_LIMIT` | `50` | 单企业等待队列 |
| `SYNC_ACCOUNT_QUEUE_LIMIT` | `20` | 单账号等待队列 |
| `SYNC_QUEUE_TIMEOUT_MS` | `1500` | 获取执行槽位最大等待 |
| `SYNC_ACCOUNT_START_INTERVAL_MS` | `200` | 同账号请求最小启动间隔 |
| `TABLE_META_DEADLINE_MS` | `8000` | 表结构内部截止时间，必须小于飞书 10 秒 |
| `RECORDS_DEADLINE_MS` | `18000` | 记录接口内部截止时间，必须小于飞书 20 秒 |
| `HTTP_REQUEST_TIMEOUT_MS` | `25000` | Node 请求生命周期保护 |
| `HTTP_HEADERS_TIMEOUT_MS` | `10000` | 请求头超时 |
| `HTTP_KEEP_ALIVE_TIMEOUT_MS` | `5000` | Keep-Alive 空闲时间 |
| `HTTP_MAX_REQUESTS_PER_SOCKET` | `1000` | 单连接最大请求数 |
| `HTTP_COMPRESSION_THRESHOLD_BYTES` | `1024` | 响应压缩阈值 |
| `UPSTREAM_RESPONSE_MAX_BYTES` | `10485760` | 单次上游响应体上限，默认 10 MB |
| `JSON_BODY_LIMIT` | `1mb` | 入站 JSON 请求体上限 |
| `SHUTDOWN_TIMEOUT_MS` | `10000` | 优雅停机最长等待 |
| `CAPTURE_RATE_LIMIT_PER_MINUTE` | `20` | 单来源捕获频率 |
| `MANAGEMENT_RATE_LIMIT_PER_MINUTE` | `120` | 管理 API 频率 |
| `RATE_LIMIT_MAX_BUCKETS` | `10000` | 单实例限流来源桶上限 |

前端仅使用 `data-sync-fe-vue-demo/.env.example` 中的 `VITE_DOUDIAN_LOCAL_AGGREGATE_HOST`。生产环境通常由同源后端托管前端，不需要把内部令牌写入前端变量。

## 4. 可信管理身份接入

生产环境所有以下路径都必须先经过可信认证网关：

- `/api/v1/connector/accounts`
- `/api/v1/connector/shared-accounts`
- `/api/v1/connector/sources/capture-session`
- `/api/v1/connector/sources/capture-status`
- `/api/v1/connector/sources/capture-clear`
- `/api/v1/connector/doudian-interfaces`
- `/api/v1/connector/doudian/test-connection`
- `/api/v1/sync`

网关必须先从可信登录会话解析企业和用户，删除客户端传入的全部同名 `X-Connector-*` Header，再注入：

| Header | 内容 |
| --- | --- |
| `X-Connector-Company-Id` | 已认证用户所属企业 ID |
| `X-Connector-User-Id` | 已认证用户 ID |
| `X-Connector-Identity-Timestamp` | 当前 Unix 毫秒时间戳 |
| `X-Connector-Identity-Nonce` | 每次请求唯一的随机值，16 至 128 位，仅允许字母、数字、下划线和短横线 |
| `X-Connector-Identity-Signature` | 下述规范串的 HMAC-SHA256 小写十六进制 |

规范串严格使用换行符连接：

```text
timestamp
nonce
UPPERCASE_HTTP_METHOD
originalUrl_with_query
sha256_hex_of_raw_request_body
companyId
userId
```

注意事项：

- `originalUrl_with_query` 必须是后端实际收到的完整转发路径和 Query，网关签名后不能再改写路径。
- 正文哈希必须基于转发给后端的原始字节，不能重新序列化 JSON。
- 网关与后端必须时钟同步；默认允许 30 秒偏差。
- nonce 必须使用密码学安全随机数且每请求唯一。后端只保存其 SHA-256，并依靠 MySQL 唯一键跨实例拒绝重放。
- 网关应保留浏览器的合法 `Origin`，或在可信服务转发时设置为 `FRONTEND_PUBLIC_URL`；同源校验仍会先于身份认证执行。
- 后端监听端口必须通过安全组、容器网络或反向代理 ACL 禁止公网绕过，`MANAGEMENT_IDENTITY_SECRET` 只能存在于网关和后端密钥管理中。
- 普通 Body、Query、Cookie 或 `X-Company-Id`、`X-User-Id` 均不能作为认证依据。

## 5. 推荐部署流程

1. 在变更窗口前备份 MySQL，并验证可恢复。
2. 停止旧实例写入，避免凭证加密迁移期间旧代码并发覆盖 Cookie。
3. 轮换所有历史敏感凭证，生成新的身份签名密钥、加密密钥和内部令牌。
4. 按第 4 节接入认证网关，确认其剥离客户端身份 Header，且后端不能被公网直连。
5. 后端执行 `npm ci --omit=dev`，前端执行 `npm ci && npm run build`。
6. 根据 `.env.example` 配置真实 `.env`，权限建议设为仅运行用户可读。
7. 先启动一个实例。启动期间会获取 MySQL advisory lock、校验 Schema、执行兼容迁移和历史 Cookie 加密。
8. 检查 `GET /healthz` 为 200，随后检查 `GET /readyz` 为 200。
9. 使用预发布飞书连接器完成：接口目录、账号绑定、测试连接、`table_meta`、多页 `records`、Cookie 过期重绑。
10. 观察同步日志、错误率和抖店风控响应后再逐步扩容。

反向代理必须启用 HTTPS，保留 `X-Request-ID`，将请求体限制设置为不小于 `JSON_BODY_LIMIT`，上游读取超时建议为 30 秒。`TRUST_PROXY_HOPS` 必须等于可信代理层数，不能盲目增大。

## 6. 容量与扩容建议

- 默认单实例最多同时执行 40 个同步请求、单企业 10 个、单账号 1 个。
- 单账号并发建议保持 1。抖店后台接口存在风控，盲目增加账号并发通常只会提高封禁和失败概率。
- `SYNC_GLOBAL_CONCURRENCY` 不应远大于 MySQL 连接数和主机可用内存。初始建议保持默认值，通过压测观察事件循环延迟、RSS、MySQL 等待和上游错误后再调整。
- 队列满或等待超过 1.5 秒时会快速返回可重试错误，避免请求无限堆积。
- 多实例部署时，并发和内存限流都是“每实例”生效，总上游并发约等于实例数乘以配置值。扩容前要同步下调单实例上限，或引入 Redis/消息队列做集群级配额。
- 多实例的 Schema 迁移已通过 MySQL advisory lock 串行化，但发布时仍建议一次只启动一个新实例。
- 飞书 SDK 两个 vendor chunk 仍约 783 KB 和 988 KB，属于上游 SDK 体积。主业务 chunk 已降至约 52 KB；若首屏性能成为指标，应评估 SDK 是否支持进一步懒加载。

## 7. 监控与告警

最低监控项：

- `/healthz` 存活状态和 `/readyz` 数据库就绪状态。
- HTTP 5xx、429、飞书协议非 0 code、`UPSTREAM_TIMEOUT`、`SYNC_BUSY`。
- `sync_logs` 的成功率、P95/P99 耗时、失败模块和分页停滞。
- `errors` 中凭证失效、非 JSON、HTTP 403 和响应过大错误。
- Node RSS、Heap、事件循环延迟、进程重启次数。
- MySQL 活跃连接、等待队列、慢查询和磁盘容量。

每次用户报错都应优先索取页面展示的 request ID，再结合服务日志定位。

## 8. 回滚方案

1. 回滚前先停止入口流量并备份数据库。
2. 数据库改动均为新增表、字段扩容或凭证加密，不建议反向删除字段。
3. `CREDENTIAL_ENCRYPTION_KEY` 一旦用于生产数据，不得在回滚时更换或丢失。
4. 只能回滚到“支持 AES-GCM 解密”的代码版本；更老版本无法读取已迁移的 Cookie。
5. 若新版本异常，恢复上一份支持加密的应用包，保持同一 `.env`，检查 `/readyz` 后再恢复流量。
6. 若 DDL 迁移失败，不要手工删表。根据启动错误、备份和 `INFORMATION_SCHEMA` 制定兼容 SQL。

## 9. 验证结果

2026-07-14 已执行：

- 后端 `npm test`：28 项全部通过，包含 100 请求突发背压回归。
- 后端 `npm run check`：通过。
- 前端 `npm run build`：通过，只有飞书 SDK 大 chunk 警告。
- 使用不含真实凭证的完整合成变量执行生产配置校验：通过。
- 当前真实 `.env` 生产配置校验：首先因缺少 `SECRET_KEY` 被正确阻止，未改写该文件。
- 根目录 `git diff --check`：通过。
- 后端 `npm audit --registry=https://registry.npmjs.org`：0 个已知漏洞。
- 前端 `npm audit --registry=https://registry.npmjs.org`：0 个已知漏洞。
- 当前工作树 61 个文件密钥扫描：0 个待复核项，已排除真实 `.env`，扫描过程未输出任何值。
- Git 历史增强扫描：发现历史路径中存在疑似长 Cookie 和硬编码环境变量回退值，扫描过程未输出任何值。
- 本次功能闭环后再次执行后端 `npm run check`：通过。
- 本次功能闭环后再次执行前端 `npm run build`：通过，仍只有飞书 SDK 大 chunk 警告。
- 本次功能闭环后再次执行根目录 `git diff --check`：通过。

本轮未执行：

- 未使用真实 MySQL 完成启动和 Schema 迁移，因为当前真实 `.env` 未满足新安全校验。
- 未调用真实抖店账号执行端到端同步，避免使用或泄露现有生产凭证。
- 未执行正式压力测试。上线前应使用脱敏测试账号验证 1 倍、2 倍预期峰值和持续 30 分钟稳定性。
- 按当前任务范围未再次执行完整 `npm test` 或集中测试，仅进行了语法、类型和构建级轻量验证。

## 10. Git 历史清理建议

当前工作树已不再跟踪真实运行数据，但仅删除最新版本文件不能清除 Git 历史。上线前必须先轮换所有相关凭证，再在完整备份和团队确认后使用 `git filter-repo` 或托管平台的敏感数据清理流程重写历史、强制推送，并通知所有开发者重新克隆。

不要把“重写历史”当作凭证轮换的替代品：历史对象、开发者本地仓库、CI 缓存和镜像都可能保留旧值。
